# 結合圖神經網路與 Kolmogorov-Arnold 表徵網路之微服務系統根因分析：可解釋性與泛化能力之研究

## 摘要
本研究針對現代微服務系統中根因分析（Root Cause Analysis, RCA）面臨之可解釋性不足與泛化能力有限等問題，提出一結合圖神經網路（Graph Neural Network, GNN）與 Kolmogorov-Arnold Networks（KAN）之創新架構——GNN_KAN。有別於傳統僅仰賴結構學習或統計相關性之方法，本研究透過圖結構建模異常傳播路徑，並以 KAN 模組取代傳統多層感知器（MLP）中之黑箱非線性轉換，利用 Kolmogorov-Arnold 表徵定理將多變量函數分解為可學習之一維基底函數組合，使模型兼具表達力與可解釋性。本研究進一步提出「RCA-aware」特徵設計原則，系統化地定義可用於根因定位之特徵，避免資料洩漏與分布偏移。實驗結果顯示，GNN_KAN 在五個核心資料集（包含代碼級故障、多源遙測數據、標準化評估基準）上，於 Top-k 準確度、Precision@k 等指標均展現優異性能，特別在代碼級故障場景達到完美或近完美之識別率。本研究亦提出系統化之基底函數選擇策略，根據系統拓樸結構、故障類型與資料特性提供實務部署之指導原則。

**關鍵詞**：根因分析、圖神經網路、Kolmogorov-Arnold Networks、微服務系統、可解釋性人工智慧、可觀測性

---

## 1. 緒論

### 1.1 研究背景與動機
隨著雲端運算與容器化技術的普及，現代軟體系統日趨複雜，由數十至上百個微服務構成的分散式架構已成為常態。在這樣的環境中，當系統發生異常（如延遲飆升、錯誤率激增、交易失敗），運維工程師需要在龐大的觀測資料與錯綜複雜的服務依賴中快速定位根本原因（Root Cause），以恢復服務並防止損失擴大。然而，現代分散式系統的根因分析（Root Cause Analysis, RCA）面臨前所未有的挑戰：

首先，**高維度與多源資料**使問題空間龐大。每個服務產生多維度的觀測信號（metrics、logs、traces），數十個服務即可累積上千維度的時序資料，傳統單變量統計方法難以捕捉多變量間的關聯與交互作用。

其次，**異常傳播與依賴耦合**使根因識別困難。在呼叫鏈中，上游服務的異常（如資料庫慢查詢）會迅速沿著依賴關係向下游傳播，導致多個下游服務同時出現異常表徵（如延遲上升、錯誤率增加）。這種「級聯效應」會掩蓋真正的根因，使下游的明顯異常反而誤導診斷方向。

第三，**動態拓樸與雜訊觀測**增加不確定性。微服務架構的依賴關係可能隨版本更新、流量路由、彈性擴縮而動態變化；觀測信號本身包含雜訊、尖峰、週期性波動與負載相關的變異，使異常偵測與根因推論都面臨穩健性挑戰。

第四，**可解釋性與實務可行性**是部署的關鍵需求。工程團隊不僅需要「哪個服務是根因」的答案，更需要理解「為何這個服務被判定為根因」與「異常如何傳播至其他元件」。黑箱模型即使準確度高，若缺乏可解釋的路徑與證據，在實務上仍難以被信任與採納。

### 1.2 相關研究之限制
現有根因分析方法可歸納為三大類別，各具不同限制：

**規則與啟發式方法**仰賴領域專家知識與預先定義之依賴圖結構，透過閾值檢測與路徑追蹤進行根因推論。此類方法具備良好之可解釋性，然面對複雜動態場景時，手工規則難以涵蓋所有異常模式，且規則維護成本隨系統演化而持續增加，可擴展性有限。

**統計與關聯分析方法**（如 Pearson 相關係數、Granger 因果檢定、變化點偵測）試圖從觀測資料中自動發現異常關聯性。然而，此類方法面臨「相關性不等於因果性」之根本問題：在多跳傳播與共同原因干擾下，統計相關性可能誤將高度相關之下游節點判定為根因。此外，這些方法對觀測雜訊與非平穩性較為敏感，且通常假設線性或簡化之因果結構，難以捕捉複雜之非線性傳播模式。

**深度學習與圖神經網路方法**近年於結構化資料分析展現優異性能，能透過消息傳遞機制學習節點間複雜交互關係。部分研究嘗試將 GNN 應用於微服務根因分析，以服務依賴圖為基礎學習節點表徵與根因分數。然而，現有 GNN 方法仍面臨兩大關鍵挑戰：（1）**可解釋性不足**——傳統 GNN 使用黑箱非線性啟動函數（如 ReLU），難以驗證模型決策是否遵循合理之因果傳播路徑；（2）**泛化能力受限**——模型易過度擬合訓練資料之特定分布與拓樸結構，面對業務變更、負載偏移或新工作負載時，性能顯著下降。

### 1.3 研究貢獻
為應對上述挑戰，本研究提出 **GNN_KAN**——一結合圖神經網路（GNN）與 Kolmogorov-Arnold Networks（KAN）之創新根因分析架構。GNN_KAN 整合 GNN 之結構推理能力與 KAN 之可解釋函數分解特性，並透過 **RCA-aware** 特徵設計原則，有效降低資料洩漏與分布偏移之風險。本研究之主要貢獻如下：

**貢獻一：可解釋之圖神經網路架構**。本研究創新地將 Kolmogorov-Arnold Networks（KAN）整合至 GNN 之消息傳遞機制中，利用 Kolmogorov-Arnold 表徵定理將多變量非線性函數分解為一維可學習函數之組合。相較於傳統 MLP 採用黑箱非線性啟動函數（如 ReLU、Sigmoid），KAN 以可參數化之一維基底函數（如 B-spline、Chebyshev 多項式、Fourier 基底、PQC_GPU）為基礎，透過少量可學習係數實現函數組合，使模型兼具資料驅動之表達能力與可解釋之函數形狀。

**貢獻二：RCA-aware 特徵工程框架**。本研究系統化地定義 RCA 場景下之特徵設計準則，專注於與根因定位高度相關且在實務部署中可穩定量測之訊號。此框架包含：（1）僅使用事件窗口內可觀測之前饋特徵，避免未來資訊洩漏；（2）聚焦可傳播之訊號與結構關係，排除與標註耦合之後驗指標；（3）採用一致之正規化與對齊策略，降低跨服務與跨場景之分布偏移影響。

**貢獻三：系統化基底函數選擇策略**。本研究基於五個核心資料集（包含代碼級故障、多源遙測數據、標準化評估基準）之實驗結果，提出系統化之基底函數選擇方法。此策略根據系統拓樸結構（扇出型/線性/深層複雜）、故障類型（資源/網路/代碼級）、資料豐富度（純指標/多源數據）及性能效率權衡，為實務部署提供明確且可操作之指導原則。

**貢獻四：全面實證評估與可重現研究流程**。本研究於多個真實微服務基準（Online Boutique、Sock Shop、Train Ticket）及 RCAEval 平台資料集上進行全面評估，涵蓋不同拓樸結構、負載模式與異常型態。實驗結果顯示，GNN_KAN 在 Top-k 準確度、Precision@k 等指標上展現優異性能，特別在代碼級故障場景達到完美或近完美之識別率。本研究於 RCAEval 平台上實作完整之資料管線、模型訓練與評估流程，確保研究之可重現性。

本文其餘章節組織如下：第 2 節回顧相關研究；第 3 節闡述問題定義與 GNN_KAN 方法論；第 4 節說明實驗架構；第 5 節呈現演算法偽程式；第 6 節分析計算複雜度與可擴展性；第 7 節描述實驗設計與設定；第 8 節報告實驗結果與分析；第 9-10 節討論研究洞察與限制；第 11 節總結研究並提出未來研究方向。

---

## 2. 相關研究

根因分析為分散式系統可靠性工程之核心問題，近年吸引學術界與產業界廣泛關注。本節回顧與本研究相關之方法類別，並闡述其與 GNN_KAN 之關聯性與差異性。

### 2.1 圖結構導向之根因分析
多項研究利用服務依賴圖或呼叫圖建模異常傳播過程。例如，基於 PageRank 之方法將異常分數視為圖上之隨機遊走過程；基於路徑分析之方法則追蹤呼叫鏈並累積路徑上之異常證據。此類方法之優勢在於直觀性與可解釋性，然面臨以下挑戰：（1）如何學習或初始化邊權重以精確反映真實傳播強度；（2）如何有效整合時序資訊與靜態拓樸結構；（3）對圖結構不完整或含雜訊時之穩健性不足。GNN_KAN 繼承圖結構建模之思想，並透過可學習之消息傳遞機制與 KAN 之非線性轉換，實現自適應之邊權與節點表徵調整，同時以正則化技術控制過度擬合問題。

### 2.2 統計與關聯分析方法
傳統統計方法（如 Pearson 相關係數、互資訊、Granger 因果檢定）試圖從觀測時序中發掘變量間之關聯性或因果關係。變化點偵測與異常偵測技術亦常用於識別時序中之突變特徵。然而，此類方法面臨「相關性不等於因果性」之根本限制：在多跳傳播場景中，下游節點與系統異常之統計相關性可能高於上游真實根因，導致誤判。此外，這些方法對觀測雜訊、非平穩性與混疊效應較為敏感，且通常假設線性或簡化之因果結構，難以捕捉複雜之非線性傳播模式。GNN_KAN 透過結構化之消息傳遞與多層聚合機制，能有效區分直接影響與間接傳播，並以 KAN 模組捕捉非線性關係，克服傳統統計方法之限制。

### 2.3 因果發現與推論 (Causal Discovery in Operations)
因果推論方法（如結構方程模型、約束導向因果學習、基於獨立性測試的演算法）試圖從觀測資料中重建因果圖，進而識別根因。這類方法在可解釋性上具優勢，但在高頻、高維、非平穩的運維場景中面臨計算成本高、樣本需求大、假設難以滿足（如因果充分性、平穩性）等挑戰。GNN_KAN 並非完全替代因果方法，而是提供一種在結構不確定下的近似推理框架；未來可與因果發現結合，以因果圖作為 GNN 的結構先驗或邊權初始化。

### 2.4 深度學習與 GNN 於運維 (GNN and Deep Learning for AIOps)
近年深度學習被引入運維領域，包括異常偵測、故障預測、根因定位等任務。圖神經網路（GNN）因能處理結構化資料而受到關注，已有研究將 GNN 應用於微服務 RCA，透過消息傳遞學習節點表徵與根因分數。然而，現有 GNN 方法多使用標準的非線性層（如 ReLU、GeLU），缺乏可解釋性；且在訓練資料有限或分布偏移時，泛化能力不足。GNN_KAN 的創新在於以 KAN 取代黑箱非線性，將領域知識以基底函數形狀嵌入模型，兼顧表達力與可解釋性，並透過 RCA-aware 特徵設計提升穩健性。

### 2.5 知識引導與可解釋學習 (Knowledge-Guided and Interpretable Learning)
知識引導學習（如 Physics-Informed Neural Networks, Symbolic Regression）試圖將領域知識以物理方程、符號約束或先驗結構注入模型，以提升樣本效率與泛化能力。可解釋機器學習（如基於樹的模型、注意力機制、可視化技術）則著重於讓模型決策透明化。Kolmogorov-Arnold Networks（KAN）是近期基於 Kolmogorov-Arnold 表徵定理提出的神經網路架構，將多變量函數分解為可學習的一維函數組合，使非線性轉換既具表達力又可解釋。GNN_KAN 將 KAN 引入圖神經網路的消息傳遞中，是首次將 Kolmogorov-Arnold 表徵應用於分散式系統根因分析的研究，填補了可解釋 GNN 於 RCA 領域的空白。

### 2.6 本研究的定位 (Positioning of This Work)
綜上所述，GNN_KAN 立足於 graph-based RCA 與 knowledge-guided 深度學習的交會點，將可解釋基底函數注入 GNN 的消息傳遞過程，並以 RCA-aware 的特徵設計確保實務可行性。相較於傳統統計方法，GNN_KAN 能捕捉非線性與多跳傳播；相較於黑箱 GNN，GNN_KAN 提供可解釋的函數形狀與路徑權重；相較於因果方法，GNN_KAN 在結構不確定與高雜訊環境下更為穩健與高效。

---

## 3. 方法論

本節首先嚴謹定義根因分析問題，明確輸入、輸出與主要挑戰；隨後闡述 GNN_KAN 之整體架構及各模組如何協同解決 RCA 問題；最後詳述圖建模策略、KAN 模組設計、RCA-aware 特徵工程與學習目標。

### 3.1 問題定義與挑戰

**根因分析（Root Cause Analysis, RCA）**旨在當分散式系統發生異常事件時，從大量可能之故障點中識別最可能觸發該異常之根本原因。形式化而言，給定以下輸入：

- **節點集合** V = {v₁, v₂, ..., vₙ}，對應系統中的服務或元件（如微服務、資料庫、訊息佇列等）；
- **有向邊集合** E ⊆ V × V，表示服務間的依賴與呼叫關係，(u, v) ∈ E 代表 u 呼叫或影響 v；
- **時間窗口** W = [t_start, t_end]，涵蓋異常事件的發生與觀測期間；
- **節點觀測** X = {x_v(t) | v ∈ V, t ∈ W}，其中 x_v(t) ∈ ℝ^d 為節點 v 在時間 t 的 d 維觀測向量，包含指標（metrics，如延遲、錯誤率、吞吐量、CPU/記憶體使用）、日誌統計（logs，如異常日誌數、特定錯誤碼出現次數）、追蹤資訊（traces，如請求數、呼叫延遲分布）等；
- **異常事件標識** A，指示系統在窗口 W 內發生異常（如 SLA 違反、關鍵交易失敗、整體延遲飆升）。

**研究目標**為學習一排名函數 f: (V, E, X) → ℝ^|V|，為每個節點 v 計算根因分數 s_v，並輸出按 s_v 降序排列之節點清單 R。理想情況下，真實根因節點 v* 應出現在 R 之前列（Top-k，其中 k << |V|），使運維工程師能快速定位並修復問題。

**核心技術挑戰**包括：

**挑戰一：異常傳播之級聯掩蔽效應**。根因節點之異常會沿依賴鏈向下游傳播，導致多個下游節點同時呈現異常徵兆。下游節點之異常表徵可能更為明顯（例如面向使用者之前端服務延遲飆升），然真實根因往往位於上游（例如後端資料庫慢查詢）。此級聯效應會掩蔽真正根因，使僅基於單節點異常強度之方法失效。

**挑戰二：高維度觀測與雜訊干擾**。每個節點產生多維度觀測（數十至上百維），且觀測包含雜訊、尖峰干擾、週期性波動（如日夜負載變化）及負載相關之正常變異。如何從雜訊中提取有效異常信號，並於多維度間識別關鍵特徵，為重要技術挑戰。

**挑戰三：動態拓樸與結構不確定性**。服務依賴圖並非靜態，可能隨版本更新、流量路由調整、動態擴縮而變化；且追蹤資料可能遺漏部分依賴關係（如跨網路之非同步呼叫、第三方服務依賴）。模型需在結構不確定下進行穩健推論。

**挑戰四：多根因與複雜故障模式**。實務中可能存在多個同時發生之根因（multi-root），或根因間存在交互作用；故障模式多樣（資源瓶頸、邏輯錯誤、外部依賴故障、配置錯誤等），難以用單一規則涵蓋。

**挑戰五：可解釋性與實務信任度**。運維工程師需理解「為何此節點被判定為根因」，包括支持證據（該節點之異常表徵）、傳播路徑（如何影響下游）及與其他節點之對比。黑箱模型即使準確度高，若無法提供可驗證之解釋，難以於實務中獲得採納。

**評估準則**：本研究以 Top-k 準確度（Accuracy@k, k=1,3,5）、Precision@k、Recall@k、平均準確度均值（mAP）等排名指標評估 RCA 性能。同時考量跨場景穩健性（對拓樸變化、負載偏移、異常型態變化之適應能力）與可解釋性（路徑權重、節點貢獻是否與預期傳播一致）。

### 3.2 GNN_KAN 解決方案架構

針對上述挑戰，GNN_KAN 提出整合式解決方案，透過圖神經網路與 Kolmogorov-Arnold 表徵網路之協同設計實現：

**解決方案一：基於圖消息傳遞之多跳異常傳播建模**  
本研究將微服務系統表示為動態加權有向圖 G = (V, E, w)，其中 w: E → ℝ 為邊權函數，反映異常傳播強度。透過多層圖神經網路之消息傳遞機制（message passing），節點表徵 h_v 不僅整合自身觀測 x_v，亦聚合來自鄰居節點之訊息。此機制使模型能捕捉多跳傳播特性：上游節點之表徵經多層聚合後影響下游節點，而學習所得之邊權與聚合權重可反映傳播路徑之重要性。相較於單節點分析或成對關聯分析，GNN 之結構化推理能有效區分直接影響與間接傳播，緩解下游掩蔽效應。

**解決方案二：基於 Kolmogorov-Arnold 表徵之可解釋非線性建模**  
傳統 GNN 使用固定非線性啟動函數（如 ReLU）與多層感知器（MLP），其函數形狀缺乏領域語義且難以解釋。本研究以 Kolmogorov-Arnold Networks（KAN）取代此類黑箱非線性模組：KAN 基於 Kolmogorov-Arnold 表徵定理，將多變量函數分解為一維可學習函數之組合。具體而言，KAN 以可參數化之一維基底函數（如 B-spline、Chebyshev 多項式、Fourier 基底、PQC_GPU）建構函數空間，透過學習組合係數實現函數逼近。此設計帶來雙重優勢：（1）**函數分解之可解釋性**——每個一維函數之形狀可直接視覺化與驗證，符合「異常越強、傳播越顯著」等領域直觀；（2）**降低參數複雜度與過擬合風險**——相較於高維 MLP，一維函數組合於參數效率與泛化能力上更優。

**解決方案三：RCA-aware 特徵工程框架**  
特徵設計為 RCA 成敗之關鍵因素。本研究提出「RCA-aware」設計準則：僅使用與根因定位直接相關且於實務部署中可穩定量測之特徵。具體準則包括：（1）**前饋可觀測性**——僅使用事件窗口內可前饋觀測之統計量（如延遲分位數、錯誤率、資源使用率），避免引入未來資訊或事後標註指標；（2）**傳播導向性**——聚焦可傳播之訊號（如上游延遲對下游之影響）與結構關係（依賴強度、呼叫頻率），排除與標註耦合或與根因無直接關聯之特徵；（3）**一致性正規化**——採用統一之正規化與對齊策略（如 z-score 正規化、時間對齊），降低跨服務偏差與分布偏移。此框架確保模型學習通用異常傳播模式，而非訓練集特定之偽相關。

**解決方案四：排名導向之多目標學習機制**  
本研究之訓練目標整合排名損失（如 Margin Ranking Loss）以最大化真實根因於候選清單中之排名、邊權稀疏正則以促進模型學習簡潔傳播結構（避免過度複雜連接）、以及 KAN 平滑正則以防止基底函數過度擺動（保留先驗形狀）。此多目標學習機制使模型於提升準確度之同時，維持可解釋性與泛化能力。

**整體架構概述**：給定異常事件窗口，GNN_KAN 首先將各節點觀測編碼為初始表徵 h_v^0；隨後透過 L 層消息傳遞（L=3 為典型配置），每層中邊上訊息經 KAN_edge 轉換，節點聚合鄰居訊息後，先以 KAN_node 轉換節點表徵，再以 KAN_message 處理聚合訊息並透過殘差連接整合；最終讀出層（readout）將 h_v^L 映射為根因分數 s_v，並按降序輸出排名清單。訓練階段以已標註根因窗口為監督信號，最小化排名損失與正則項；推論階段對新異常事件執行前向傳播並輸出候選清單。

以下各小節將詳細闡述各核心模組之設計。

### 3.3 圖建模與消息傳遞機制

本研究將分散式系統於異常事件窗口內表示為動態加權有向圖 $G_t = (V, E, W_t)$。節點集合 $V = \{v_1, ..., v_n\}$ 對應各服務或元件，邊集 $E \subseteq V \times V$ 由服務依賴關係與呼叫追蹤資訊萃取而得。邊權矩陣 $W_t \in \mathbb{R}^{|V| \times |V|}$ 初始化時可基於結構先驗設定，訓練過程中自適應調整以反映異常傳播之實際強度。

**初始節點編碼**：對於節點 $v_i \in V$，本研究將其於觀測窗口內之時序資料透過特徵萃取程序（第 3.5 節）轉換為固定維度特徵向量 $\bar{x}_{v_i} \in \mathbb{R}^{d_{\text{feat}}}$，再經線性變換與層正規化得到初始表徵 $h_{v_i}^{(0)} \in \mathbb{R}^{d_h}$，其中 $d_h$ 為隱藏表徵維度（典型值為 64）。

**多層消息傳遞機制**：GNN_KAN 透過 $L$ 層消息傳遞機制捕捉多跳異常傳播特性。於第 $l$ 層（$l = 1, ..., L$），依序執行以下操作：

**步驟一：邊訊息生成**。對於邊 $(v_u \to v_v) \in E$，本研究將源節點表徵 $h_u^{(l-1)}$ 與邊特徵 $e_{uv}$（包含邊權與呼叫頻率等資訊）串接後，經 KAN 模組 $\text{KAN}_{\text{edge}}$ 轉換為訊息 $m_{u \to v}^{(l)}$，此過程以可解釋之一維函數組合建模異常傳播之非線性模式。

**步驟二：節點訊息聚合**。節點 $v$ 接收所有入鄰居 $\mathcal{N}_{\text{in}}(v)$ 之訊息，透過注意力機制進行加權聚合。注意力權重 $\alpha_{uv}^{(l)}$ 根據源節點與目標節點之表徵相似度計算，使模型自動識別對節點 $v$ 影響最顯著之鄰居，反映異常傳播路徑之重要性。聚合後之訊息記為 $\tilde{m}_v^{(l)}$。

**步驟三：節點表徵更新**。節點更新程序分為兩階段：（1）以 $\text{KAN}_{\text{node}}$ 對節點表徵 $h_v^{(l-1)}$ 進行非線性轉換，得到 $\tilde{h}_v^{(l)}$；（2）以 $\text{KAN}_{\text{message}}$ 處理聚合訊息 $\tilde{m}_v^{(l)}$，並透過殘差連接（殘差權重 $\alpha=0.3$）與轉換後之表徵整合，得到更新後之表徵 $h_v^{(l)}$。此設計使模型先對節點表徵進行非線性轉換，再整合來自鄰居之訊息，最後透過殘差連接穩定訓練過程並促進梯度流動。

透過 $L$ 層疊加（依據實驗結果，$L=3$ 為典型配置），每個節點之表徵 $h_v^{(L)}$ 融合自身觀測與 $L$ 跳鄰居之訊息，使模型能有效捕捉深層依賴關係與異常傳播路徑。

**讀出層與根因評分**：最終層表徵 $h_v^{(L)}$ 經讀出層（線性變換 + sigmoid 函數）映射為根因分數 $s_v \in (0, 1)$。高分數表示節點 $v$ 更可能為根因。所有節點按 $s_v$ 降序排列，輸出候選清單 $R$。

**雙圖構建策略**：依據實作架構，本研究採用雙圖策略以整合結構相似性與異常傳播資訊：

**相似性圖 $G_{\text{sim}}$**：基於特徵餘弦相似度之無向圖，邊權 $w_{\text{sim}}(u, v)$ 定義為兩節點特徵向量之餘弦相似度減去閾值 $\tau_{\text{sim}}$（預設 0.5）後取最大值，捕捉結構相似性。

**傳播圖 $G_{\text{prop}}$**：基於時滯相關性之有向圖，捕捉異常傳播方向。對於節點對 $(u, v)$，本研究計算其異常信號之最大時滯相關性 $\rho_{\text{lag}}(u, v)$。若相關性超過閾值 $\tau_{\text{prop}}$ 且 $u$ 之異常發生時間早於 $v$，則建立有向邊 $u \to v$，權重 $w_{\text{prop}}(u, v)$ 與時滯相關性成正比，與時間差成反比，編碼時序一致性。

於前向傳播過程中，本研究對兩圖分別執行消息傳遞，得到 $h_v^{\text{sim}}$ 與 $h_v^{\text{prop}}$，最終透過 MLP 融合層整合為最終表徵 $h_v^{\text{final}}$。

此雙圖策略使 GNN_KAN 能同時利用結構相似性與異常傳播之時序因果線索，提升根因識別之準確度與穩健性。

### 3.4 Kolmogorov-Arnold Networks 模組設計

傳統神經網路使用固定形狀之啟動函數（如 ReLU、Sigmoid、Tanh）與多層感知器（MLP），此類黑箱非線性模組雖計算簡單且表達力強，然缺乏可解釋性且易過度擬合。**Kolmogorov-Arnold Networks（KAN）**基於 Kolmogorov-Arnold 表徵定理，提供可學習且可解釋之函數逼近替代方案。

**Kolmogorov-Arnold 表徵定理**（Kolmogorov, 1957; Arnold, 1957）指出：任意連續多變量函數可分解為一維函數之組合。此定理提供結構化函數逼近之理論基礎，KAN 將此數學原理應用於神經網路設計：以可參數化之一維函數取代固定之非線性啟動，透過學習函數組合實現多變量映射。

於 RCA 場景中，KAN 帶來以下優勢：
- **可解釋之函數分解**：每個一維函數可視覺化，驗證是否符合領域預期（如單調性：異常越強、傳播越顯著；門檻性：異常需超過閾值始傳播；飽和性：極端異常之影響趨於飽和）；
- **參數效率與泛化能力**：相較於高維 MLP，一維函數組合之參數量更少，降低過度擬合風險，於標註稀缺之 RCA 場景中尤為重要；
- **先驗知識注入之靈活性**：可選擇適合 RCA 之基底函數族（如 B-spline 建模平滑傳播、Chebyshev 多項式建模穩定逼近），將領域知識編碼至模型結構中。

#### 3.4.1 一維函數參數化與數學定義

KAN 之核心為將多變量函數分解為一維函數之組合。對於輸入 $z = [z_1, \ldots, z_{d_{\text{in}}}]^T$，KAN 層將多變量映射 $\phi: \mathbb{R}^{d_{\text{in}}} \to \mathbb{R}$ 分解為 $d_{\text{in}}$ 個一維函數 $\psi_i(z_i)$ 之加權和，其中每個一維函數 $\psi_i(z_i) = \sum_{k=1}^{K} \alpha_{i,k} \cdot b_k(z_i)$ 由 $K$ 個基底函數 $b_k$（典型值 $K=5-10$）與可學習係數 $\alpha_{i,k}$ 組合而成。此結構遵循 Kolmogorov-Arnold 表徵之精神，使模型兼具表達力與可解釋性。

**基底函數族選擇**：依據實作架構，本研究支援多種基底函數族，各具不同特性：

**（1）Chebyshev 多項式基底**（預設選項）：
$$
b_k^{\text{Cheb}}(z) = T_k(\text{tanh}(z))
$$
其中 $T_k(\cdot)$ 為第 $k$ 階 Chebyshev 多項式，$\text{tanh}(\cdot)$ 將輸入正規化至 $[-1, 1]$ 區間。Chebyshev 基底於該區間上正交，具備良好之逼近性質與數值穩定性。

**（2）B-spline 基底**：
$$
b_k^{\text{Spline}}(z) = B_{k,p}(z; \{t_i\}_{i=0}^{n})
$$
其中 $B_{k,p}(\cdot)$ 為 $p$ 階（通常 $p=3$ 為立方樣條）B-spline 基底函數，$\{t_i\}$ 為節點序列。B-spline 基底具備局部支撐性（每個基底僅於有限區間非零），可建模分段平滑之非線性關係。

**（3）Fourier 基底**：
$$
b_k^{\text{Fourier}}(z) = \begin{cases}
\cos(k \pi z) & \text{若 } k \text{ 為偶數} \\
\sin(k \pi z) & \text{若 } k \text{ 為奇數}
\end{cases}
$$
Fourier 基底適合捕捉週期性模式，對負載相關之週期性異常特別有效。

**（4）參數化量子電路基底（PQC_GPU）**：本研究使用 PennyLane 框架實作參數化量子電路（PQC），透過 GPU 加速之量子電路模擬捕捉複雜之非線性耦合關係。PQC_GPU 基底函數 $b_k^{\text{PQC\_GPU}}(z)$ 基於量子電路測量期望值 $\mu(z; \boldsymbol{\theta}, \boldsymbol{\phi})$ 計算，其中 $\mu$ 透過等效之三角函數模擬實現（避免逐樣本執行量子電路）。PQC_GPU 包含資料編碼層（RY 旋轉閘）、參數化層（RX/RY/RZ 旋轉閘與 CNOT 糾纏閘）與測量層，具備指數級表示能力（理論上可表示 $2^Q$ 維之量子態空間，$Q$ 為量子位元數，預設 4）。

**實作優勢**：PQC_GPU 透過 GPU 並行化實現高效之量子電路模擬，具備指數級表示能力（理論上可表示 $2^Q$ 維之量子態空間）。於 RCA 場景中，PQC_GPU 特別適合捕捉複雜之多服務協同故障、高維特徵異常與深度非線性傳播模式，於處理服務依賴鏈斷裂等複雜故障時表現優異。

#### 3.4.2 KAN 層之前向傳播機制

KAN 層之前向傳播包含三個並行計算組件：（1）**基底函數計算**：對批次輸入 $Z$ 計算所有基底函數 $b_k(Z)$，得到 $\mathcal{B}(Z) \in \mathbb{R}^{B \times d_{\text{in}} \times K}$；（2）**基底函數線性組合**：使用 Einstein 求和約定（einsum）將基底函數與可學習係數張量 $A$ 收縮，得到 $\Phi_{\text{basis}}(Z)$；（3）**可學習線性基底**：計算 $\Phi_{\text{linear}}(Z) = W_{\text{base}} \cdot Z + b_{\text{base}}$ 以保留表達力。最終輸出透過 $\tanh$ 限制範圍、縮放係數（0.5, 0.1）平衡兩組件貢獻，並以 LayerNorm 穩定訓練過程。

#### 3.4.3 平行化實作策略

依據實作架構，KAN 層之平行化透過以下技術實現：

**（1）向量化基底函數計算**：所有基底函數 $b_k(\cdot)$ 以張量運算實現，對批次 $B$ 與輸入維度 $d_{\text{in}}$ 全面並行。例如 Chebyshev 基底透過遞迴公式 $T_{k+1}(x) = 2x \cdot T_k(x) - T_{k-1}(x)$ 實現，遞迴展開為張量運算，單次前向傳播計算所有階數。

**（2）Einstein 求和優化**：使用 PyTorch 之 `torch.einsum` 進行張量收縮，其底層調用 BLAS/cuBLAS 實現矩陣乘法之 GPU 加速。對於批次大小 $B \sim 32$、基底數 $K \sim 8$、維度 $d \sim 64$，einsum 相較循環實現可達 10-50× 加速。

**（3）基底函數工廠模式**：實作採用 `BasisFunctionFactory` 設計模式，預先實例化基底函數物件並複用於所有 KAN 層，避免重複初始化開銷。基底函數參數（如 Chebyshev 之正規化係數、B-spline 之節點序列）於建構時計算並快取為 GPU 張量。

**（4）混合精度訓練**：KAN 層支援 FP16/BF16 混合精度，基底計算於 FP32 以保證數值穩定性，線性變換與聚合於 FP16 以加速，透過自動混合精度（AMP）實現。

**效能特徵**：於 NVIDIA V100 GPU 上，單個 KAN 層（$d_{\text{in}}=d_{\text{out}}=64, K=8$）之前向傳播時間約 0.3ms（批次大小 32），相較標準 MLP 層（0.2ms）增加約 50% 計算量，然仍滿足線上推論需求（整體模型推論 < 10ms）。

#### 3.4.4 領域先驗注入與可解釋性分析

**領域先驗知識注入**：透過選擇基底函數類型與初始係數，本研究可注入領域知識。例如：
- 若預期傳播具單調性，初始化 $\alpha_{i,k}$ 使一維函數 $\psi_i$ 保持單調遞增（如選擇 Chebyshev 奇數階多項式並限制係數符號）；
- 若預期存在激活門檻，選擇 B-spline 基底並於閾值區間配置更密集之節點，或選擇 sigmoid 型基底；
- 若預期飽和行為，於一維函數輸出後施加 $\tanh(\cdot)$ 或選擇有界基底（如 Fourier 基底）。

**可解釋性與可視化分析**：訓練完成後，每個 KAN 模組之一維函數 $\psi_i(x_i) = \sum_{k} \alpha_{i,k} \cdot b_k(x_i)$ 可獨立繪製。對於每個輸入維度 $i$，本研究於 $x_i \in [-3, 3]$（涵蓋 99% 正規化輸入）採樣並計算 $\psi_i(x_i)$，直觀展示該維度如何被非線性轉換。分析結果顯示：
- **邊 KAN**：一維函數多呈現單調遞增或 S 形曲線，符合「異常越強，傳播越顯著」與「門檻激活」之 RCA 預期；
- **節點 KAN**：一維函數呈現更多樣之形狀，反映不同服務對訊息之處理邏輯差異。相較之下，標準 ReLU 無法提供此類洞察。

相較於黑箱 MLP，KAN 之一維函數分解使本研究能驗證模型是否學習到合理之傳播邏輯，增強運維工程師對模型之信任與除錯能力。

**於 GNN_KAN 中之應用位置**：

1. **KAN_edge**：於邊訊息生成階段（第 3.3 節公式），將 $[h_u^{(l-1)} \oplus e_{uv}]$ 經 KAN 轉換為訊息 $m_{u \to v}^{(l)}$，以一維函數組合建模異常傳播之非線性模式；

2. **KAN_node**：於節點更新階段，先對 $h_v^{(l-1)}$ 進行 KAN 轉換得到 $\tilde{h}_v^{(l)}$，再以 **KAN_message** 處理聚合訊息 $\tilde{m}_v^{(l)}$，最後透過殘差連接整合，以一維函數組合建模節點內部之非線性整合邏輯。

透過 KAN，GNN_KAN 之消息傳遞不再為黑箱 MLP，而是遵循 Kolmogorov-Arnold 表徵之可解釋函數分解，兼顧模型之表達力（逼近任意連續函數）、可解釋性（一維函數可視化）與穩健性（降低參數複雜度與過度擬合）。此設計使 GNN_KAN 能於有限標註下學習合理之傳播模式，並於分布偏移時保持穩健。

### 3.5 RCA-aware 特徵工程框架

特徵設計為 RCA 模型成功之關鍵因素，不當之特徵會引入資料洩漏（data leakage）或分布偏移（distribution shift），導致訓練集高準確度然部署時失效。本研究提出 **RCA-aware** 特徵設計原則，並基於實作架構提供系統化定義。

#### 3.5.1 特徵設計之四大準則

**準則一：僅使用前饋可觀測特徵（Forward-Observable Features）**  
特徵必須於異常事件窗口 $W = [t_{\text{start}}, t_{\text{end}}]$ 內即時可得，不依賴未來資訊或事後標註。形式化而言，特徵萃取函數 $\mathcal{F}$ 必須滿足因果約束：
$$
\bar{x}_v = \mathcal{F}\left( \{x_v(t) | t \in W\}, \{x_u(t) | u \in \mathcal{N}(v), t \in W\} \right)
$$
其中 $\mathcal{N}(v)$ 為節點 $v$ 之鄰居集合。禁止使用需事後回溯之指標或與標註耦合之資訊。

**準則二：聚焦可傳播訊號與結構關係（Propagation-Centric Signals）**  
RCA 之核心為理解異常如何傳播，因此特徵應捕捉傳播相關之訊號。本研究將特徵分為三類：
- **異常強度特徵**：量化節點偏離正常狀態之程度；
- **時序變化特徵**：捕捉異常之動態特性與變化點；
- **傳播關聯特徵**：量化節點間之異常關聯性與時滯。

**準則三：排除標註耦合之後驗指標（No Label-Coupled Features）**  
若特徵函數 $\mathcal{F}$ 依賴標註 $y$，即 $\bar{x}_v = \mathcal{F}(x_v, y)$，則為洩漏特徵。本研究透過靜態分析與動態驗證確保 $\mathcal{F}$ 與 $y$ 獨立。

**準則四：一致之正規化與對齊策略（Consistent Normalization and Alignment）**  
對於每個觀測維度 $x_v^{(d)}(t)$，本研究基於歷史正常期 $\mathcal{T}_{\text{normal}}$ 計算 z-score 正規化：
$$
\tilde{x}_v^{(d)}(t) = \frac{x_v^{(d)}(t) - \mu_v^{(d)}}{\sigma_v^{(d)} + \epsilon}
$$
其中 $\mu_v^{(d)} = \mathbb{E}_{t \in \mathcal{T}_{\text{normal}}}[x_v^{(d)}(t)]$ 為正常期均值，$\sigma_v^{(d)} = \text{Std}_{t \in \mathcal{T}_{\text{normal}}}[x_v^{(d)}(t)]$ 為正常期標準差，$\epsilon = 10^{-8}$ 為數值穩定項。

#### 3.5.2 系統化特徵定義

依據實作架構（`rca_aware_metric_processing`），本研究對每個服務節點 $v$ 萃取以下特徵向量 $\bar{x}_v \in \mathbb{R}^{d_{\text{feat}}}$（$d_{\text{feat}} = 48$）：

**（1）SLI 關聯特徵**（3 維）：量化節點與系統級服務水平指標（SLI）之關聯性。本研究計算同步相關性 $f_1^{(v)}$（節點與 SLI 之即時關聯）、最大時滯相關性 $f_2^{(v)}$（捕捉傳播延遲）與最佳滯後量 $f_3^{(v)}$（傳播時間估計），其中時滯相關性透過滑動窗口計算不同時間延遲 $\tau$ 下之相關係數，取最大值與對應延遲。

**（2）時序差異特徵**（4 維）：量化故障前後之變化。將觀測窗口分為故障前期與故障後期，計算統計量變化：均值差異 $f_4^{(v)}$、標準差差異 $f_5^{(v)}$、線性趨勢斜率差異 $f_6^{(v)}$（最小二乘法擬合）與異常分數差異 $f_7^{(v)}$（基於孤立森林或 z-score 閾值）。

**（3）異常偵測特徵**（2 維）：量化異常程度。計算最大 z-score $f_8^{(v)}$（峰值異常強度）與平均 z-score $f_9^{(v)}$（持續異常程度），其中 z-score 基於故障前期之均值與標準差計算。

**（4）故障類型特化特徵**（5 維/類型）：針對不同指標類型之專門特徵。對於延遲類指標，本研究額外計算：分位數差異（P99、P95）、變異係數（CV）、一階差分之標準差（變化劇烈程度）與偏態（分布偏移），以捕捉延遲指標之特殊異常模式。

對於錯誤率、CPU、記憶體等指標，本研究使用簡化之通用特徵（因實驗顯示其區分度較低）。

**（5）填充與標準化**（至目標維度）

所有特徵串接後，若維度不足 $d_{\text{feat}}$（目標維度 48），補零填充；若超過，截斷至前 $d_{\text{feat}}$ 維。最後對整個特徵矩陣按列進行 z-score 標準化，並處理 NaN/Inf 值（替換為 0.0 並限制於 $[-5.0, 5.0]$ 區間內），確保數值穩定性。

#### 3.5.3 特徵萃取之連貫性與整合

上述特徵設計與第 3.3 節之圖建模、第 3.4 節之 KAN 緊密整合：

- **與圖建模之整合**：SLI 關聯特徵（$f_1, f_2, f_3$）與傳播圖之邊權（$w_{\text{prop}}$）共享時滯相關性計算邏輯，確保節點特徵與圖結構之一致性；
- **與 KAN 之整合**：異常偵測特徵（$f_8, f_9$）提供 z-score 作為 KAN 輸入，KAN 之單調性先驗使其能穩健地將 z-score 映射為異常傳播強度；
- **與訓練目標之整合**：時序差異特徵（$f_4\sim f_7$）捕捉故障前後之變化，與排名損失之目標（識別變化最顯著之根因節點）直接對應。

此系統化特徵設計確保 GNN_KAN 於多樣化場景中學習通用異常傳播模式，而非過度擬合特定訓練集，為模型穩健性與泛化能力之基石。

### 3.6 訓練目標與學習機制

GNN_KAN 之訓練目標整合多個子目標，兼顧準確度、可解釋性與穩健性：

**主要目標：排名損失（Ranking Loss）**  
RCA 之核心為排名而非分類，因此本研究採用排名導向之損失函數。給定一標註根因為 $v^*$ 之異常事件，本研究希望 $s_{v^*}$ 顯著高於其他節點。常用之排名損失包括：
- **Margin Ranking Loss**：$L_{\text{rank}} = \sum_{v \neq v^*} \max(0, \text{margin} - (s_{v^*} - s_v))$，鼓勵根因分數與非根因分數之差距大於 $\text{margin}$；
- **ListNet / ListMLE**：基於排列機率之損失，最大化正確排名之對數似然；
- **Binary Cross-Entropy with Ranking Weight**：將問題視為二分類（是否為根因），然對排名前列之錯誤賦予更高權重。

本研究採用 Margin Ranking Loss 或其變體，因其直觀且對 Top-k 指標優化有效。

**正則項一：邊權稀疏化（Edge Sparsity Regularization）**  
為避免模型學習過於複雜之傳播結構，本研究對邊權施加 L1 正則，鼓勵模型學習稀疏之關鍵傳播路徑，提升可解釋性（僅少數邊權重高，代表主要傳播方向）並降低過度擬合風險。

**正則項二：KAN 平滑化（KAN Smoothness Regularization）**  
為防止 KAN 基底係數過度擺動導致非線性函數不穩定，本研究對係數施加平滑約束，保留基底函數之先驗形狀（如 B-spline 之光滑性），避免過度擬合並提升跨場景泛化能力。

**半監督擴展（選用）**  
於標註稀缺場景，可引入半監督目標：
- **一致性正則**：對同一事件之微擾版本（如加噪、子圖抽樣），模型輸出應保持一致；
- **結構正則**：利用圖結構先驗（如社群偵測、路徑分析）作為偽標籤或軟約束。

**總損失函數**：$L_{\text{total}} = L_{\text{rank}} + \lambda_1 \, L_{\text{edge}} + \lambda_2 \, L_{\text{kan}}$，其中 $\lambda_1=0.01$、$\lambda_2=0.001$ 為正則係數。

**優化策略**：本研究使用 Adam 或 AdamW 優化器，學習率採用 warm-up + cosine decay 排程以穩定訓練過程。批次化處理多個異常事件窗口，每批隨機抽樣事件並對應之圖結構。對於大規模圖，採用子圖抽樣或鄰居採樣技術以控制記憶體與計算成本。

透過此多目標學習機制，GNN_KAN 於提升 Top-k 準確度之同時，維持邊權與 KAN 函數之可解釋性與穩健性，確保模型於訓練與部署場景間之一致性。

## 4. 實驗架構 (Experimental Architecture)

本節說明以 RCAEval 平台實現之端到端實驗架構，確保方法論、資料處理與評估協定的一致性與可重現性。

### 4.1 基礎資料集特性 (Base Dataset Characteristics)

本研究的實驗基於五個核心資料集系列，涵蓋不同規模、拓樸結構與故障型態的微服務系統，確保評估的全面性與代表性：

**Sock Shop 系列**：
- **sock-shop-1**：Sock Shop 微服務系統第一版，包含約 10 個核心服務（前端、目錄、購物車、訂單、支付、用戶管理等），採用線性拓樸結構，服務依賴關係相對簡單。此資料集包含完整的響應時間（RTT）數據，適合測試模型在中等規模、淺層依賴場景下的根因識別能力。故障類型涵蓋資源故障（CPU、記憶體、磁碟）與網路故障（延遲、封包遺失），共計數十個標註的故障案例。
- **sock-shop-2**：Sock Shop 微服務系統第二版，在 v1 基礎上擴展了 RTT 數據的覆蓋範圍與時間跨度，提供更豐富的時序觀測資料。相較於 v1，v2 增加了更多服務間交互的追蹤資訊，使圖結構更完整，適合驗證模型在資料品質提升下的性能改善。

**RE1 系列**（Reproducible Evaluation 1）：
RE1 是 RCAEval 平台提供的標準化評估資料集，包含三個子資料集：
- **RE1-OB**（Online Boutique）：基於 Google 開源的 Online Boutique 電商微服務系統，包含約 10-15 個服務，採用典型的扇出型拓樸（前端服務同時依賴多個後端服務）。此資料集包含 375 個故障案例，涵蓋 3 種資源故障類型（CPU、記憶體、磁碟）與 2 種網路故障類型（延遲、封包遺失），每個服務提供 49-212 個觀測指標。RE1-OB 的故障注入策略遵循真實場景的異常模式，適合評估模型在電商類應用的根因分析能力。
- **RE1-SS**（Sock Shop）：基於 Sock Shop 系統的標準化資料集，包含約 10 個服務，採用線性拓樸。此資料集同樣包含 375 個故障案例，故障類型與 RE1-OB 一致，但服務拓樸與依賴模式不同，適合測試模型對不同拓樸結構的適應性。
- **RE1-TT**（Train Ticket）：基於 Train Ticket 火車票訂票系統，包含 40+ 個微服務，拓樸深度與廣度較大，依賴關係錯綜複雜。此資料集挑戰模型在大規模、深層依賴場景下的多跳傳播推理能力，是評估可擴展性的關鍵資料集。

**RE2 系列**（Reproducible Evaluation 2）：
RE2 是 RCAEval 平台提供的擴展評估資料集，在 RE1 基礎上增加了多源遙測數據（metrics、logs、traces）：
- **RE2-OB**：包含 270 個故障案例，涵蓋 4 種資源故障類型（CPU、記憶體、磁碟、Socket）與 2 種網路故障類型，每個服務提供 77-376 個觀測指標，並包含 8.6-26.9 百萬條日誌記錄與 39.6-76.7 百萬條追蹤記錄。此資料集的規模與複雜度顯著高於 RE1，適合評估模型在多源數據融合與大規模場景下的性能。
- **RE2-SS**：基於 Sock Shop 的擴展資料集，同樣包含多源遙測數據，規模為大型（large scale），適合測試模型在中等規模系統但資料豐富場景下的表現。
- **RE2-TT**：基於 Train Ticket 的擴展資料集，規模為超大型（very large scale），包含最複雜的服務拓樸與最豐富的觀測數據，是評估模型極限性能的關鍵資料集。

**RE3 系列**（Reproducible Evaluation 3）：
RE3 專注於代碼級故障（code-level faults），包含 5 種代碼級故障類型（F1-F5），共計 90 個故障案例：
- **RE3-OB/SS/TT**：三個子資料集分別基於 Online Boutique、Sock Shop 與 Train Ticket 系統，每個服務提供 68-322 個觀測指標，並包含 1.7-2.7 百萬條日誌與 4.5-4.7 百萬條追蹤記錄。代碼級故障相較於資源與網路故障更為細粒度，需要模型捕捉更微妙的異常信號，適合評估 GNN_KAN 在精細根因定位上的能力。

**資料集選擇策略**：
本研究以 **sock-shop-1**、**sock-shop-2**、**RE1**、**RE2**、**RE3** 作為基礎資料集，涵蓋了從中等規模到超大規模、從簡單拓樸到複雜拓樸、從資源/網路故障到代碼級故障的完整評估光譜。此選擇策略確保 GNN_KAN 的評估具備：
1. **規模多樣性**：從 10 個服務到 40+ 服務，測試模型的可擴展性；
2. **拓樸多樣性**：從線性拓樸到扇出型、深層依賴，測試模型的結構適應性；
3. **故障多樣性**：從資源瓶頸、網路異常到代碼錯誤，測試模型的泛化能力；
4. **資料豐富度多樣性**：從純指標數據到多源融合（metrics+logs+traces），測試模型的多模態處理能力。

### 4.2 實驗流程架構

本研究之實驗流程遵循標準化管線，確保方法論、資料處理與評估協定之一致性與可重現性。完整流程如圖 X（實驗流程圖）所示，包含以下主要階段：

**階段一：資料載入與預處理**
- **資料載入**：由 RCAEval 平台之 Datasets/Registry 管理，明確標示資料集版本、切分比例（訓練/驗證/測試：60%/20%/20%）與來源，確保事件間之時間獨立性以避免資料洩漏。
- **資料驗證**：檢查資料完整性、時間序列連續性與標註一致性。

**階段二：特徵工程與圖構建**
- **特徵處理**：由 Features/Processors 實作，落實第 3.5 節之 RCA-aware 原則（前饋可觀測、傳播導向、無標註耦合、一致正規化），對每個服務節點提取 48 維特徵向量。
- **圖構建**：由 Graphs/Builders 完成，實作雙圖策略（相似性圖與傳播圖）與時滯相關性計算（第 3.3 節），並輸出標準化之圖張量（節點特徵矩陣、邊索引、邊權重）。

**階段三：模型訓練**
- **模型初始化**：根據設定檔初始化 GNN_KAN 模型，包含多層消息傳遞（L=3）與 KAN 模組，KAN 基底函數類型（Chebyshev、B-spline、Fourier、PQC_GPU）由設定檔指定。
- **訓練執行**：採用排名損失（Margin Ranking Loss）與正則項（邊稀疏正則 λ₁=0.01、KAN 平滑正則 λ₂=0.001），使用 Adam 優化器與 warm-up + cosine decay 學習率排程。
- **驗證與早停**：於驗證集上監控性能，實施早停機制避免過度擬合。

**階段四：評估與分析**
- **推論執行**：對測試集執行前向傳播，計算各節點之根因分數並生成排名清單。
- **指標計算**：計算 Top-k Accuracy、Precision@k、Recall@k、mAP、MRR 等評估指標。
- **結果輸出**：生成結構化產物，包含模型權重、評估指標（CSV/JSON）、Top-k 排名清單、KAN 函數曲線可視化、邊權熱圖與完整日誌。

**流程圖說明**：實驗流程圖（見「GNN_KAN 實驗流程圖」之圖片）展示從資料載入至結果輸出之完整流程，包含各階段之輸入輸出、處理模組與決策點，便於理解與重現實驗過程。

### 4.3 模型與學習設定

**模型架構**：GNN_KAN 包含多層消息傳遞（L=3）與 KAN 模組（KAN_edge、KAN_node、KAN_message），KAN 基底函數類型（Chebyshev、B-spline、Fourier、PQC_GPU）與係數之設置受設定檔控制。

**訓練機制**：採用排名損失（Margin Ranking Loss），並加入邊稀疏正則（λ₁=0.01）與 KAN 平滑正則（λ₂=0.001）。優化器使用 Adam，學習率採用 warm-up + cosine decay 排程，批次大小為 32。

**隨機性控制**：透過平台統一設定隨機種子，涵蓋資料抽樣、權重初始化與後端運算，確保實驗可重現性。

### 4.4 實驗設定與執行

**設定檔管理**：以 YAML/JSON 格式統一描述資料集路徑、超參數（學習率 0.001、批次大小 32、表徵維度 64、KAN 基底數 8）、訓練排程（warm-up + cosine decay）與評估協定（Top-k 指標、mAP、MRR）。

**執行器**：Runner 統一啟動訓練與測試，支援單資料集單方法、多資料集批量執行與交叉驗證模式，並輸出結構化產物。

**產物管理**：產物包含模型權重（.pth）、評估指標（CSV/JSON）、Top-k 排名清單、KAN 函數曲線可視化、邊權熱圖、完整日誌（含環境資訊、套件版本、執行時間）。

### 4.5 重現步驟 (Reproducibility Steps)

1) 從 RCAEval 平台下載指定資料集（sock-shop-1/2、RE1/2/3）至 `data/` 目錄；
2) 指定資料集名稱與設定檔路徑，啟動 Runner 執行特徵處理、圖構建與模型訓練；
3) 於平台輸出目錄（`output/results/`）取得評估指標與可視化產物；
  4) 以相同設定於其他資料集或切分重跑，即可橫向比較或進行遷移測試。

上述實驗架構確保 GNN_KAN 的評估具備方法論嚴謹性、資料處理一致性與結果可重現性，為後續實驗結果分析提供堅實基礎。

---

## 5. 演算法設計（偽程式）(Algorithms - Pseudocode)

本節以偽程式形式呈現 GNN_KAN 的核心演算法，包含訓練流程、推論流程與 KAN 模組的操作邏輯。這些偽程式抽象化了技術細節，專注於演算法的結構與流程，便於理解與重現。
```text
Input: Graph G=(V,E), node features X, labels Y (root-cause nodes),
       KAN bases B, training windows T
Output: Trained parameters Θ = {GNN weights, edge weights, KAN coefficients}

Initialize Θ with structural priors and KAN base coefficients
for epoch = 1..E:
  for each window t in T:
    # Graph encoding
    for v in V:
      h_v^0 ← Encode(X_v(t))
    for l = 1..L:              # message passing layers
      for (u→v) in E:
        m_{u→v} ← KAN_edge(B, h_u^{l-1}, e_{u→v})
      for v in V:
        m_agg ← Aggregate({m_{u→v} | (u→v)∈E})
        h_transformed ← KAN_node(B, h_v^{l-1})
        m_processed ← KAN_message(B, m_agg)
        h_v^l ← h_transformed + α · m_processed

    # Scoring and loss
    s_v ← Readout(h_v^L)
    L_rank ← RankingLoss(s, Y(t))
    L_edge ← EdgeSparsity(e)
    L_kan  ← KANSmoothness(Θ_KAN)
    L_total ← L_rank + λ1 L_edge + λ2 L_kan

    # Parameter update
    Θ ← Θ - η · ∇_Θ L_total
```

### 5.2 推論與根因排序 (Inference and Ranking)

推論流程接收新的異常事件，執行前向傳播計算各節點的根因分數，並輸出排序後的候選清單。此過程不需標註，純粹基於觀測與學習到的模型參數。

```text
Input: Graph G=(V,E), node features X at window t, trained Θ
Output: Ranked list of suspected root-cause nodes R

for v in V:
  h_v^0 ← Encode(X_v(t))
for l = 1..L:
  for (u→v) in E:
    m_{u→v} ← KAN_edge(B, h_u^{l-1}, e_{u→v})
  for v in V:
    m_agg ← Aggregate({m_{u→v}})
    h_transformed ← KAN_node(B, h_v^{l-1})
    m_processed ← KAN_message(B, m_agg)
    h_v^l ← h_transformed + α · m_processed
for v in V:
  s_v ← Readout(h_v^L)
R ← SortByScoreDescending({(v, s_v)})
return R
```

### 5.3 KAN 模組骨架 (KAN Module Skeleton)

KAN 模組將輸入經過可解釋基底函數的組合進行非線性轉換。基底函數係數在訓練中學習，但基底本身的形狀（如 B-spline 的光滑性）保持約束。

```text
Function KAN_node(B, h):
  # h: node representation from previous layer
  φ(h) ← Σ_k α_k · b_k(h)    # b_k ∈ B are basis functions, α_k are learnable
  return Linear(φ(h))

Function KAN_message(B, m):
  # m: aggregated message from neighbors
  ψ(m) ← Σ_k β_k · b_k(m)
  return Linear(ψ(m))

Function KAN_edge(B, h, e):
  # h: source embedding, e: edge weight/attributes
  z ← Linear([h, e])
  ψ(z) ← Σ_k β_k · b_k(z)
  return ψ(z)
```

---

## 6. 複雜度與可擴展性 (Complexity and Scalability)

理解 GNN_KAN 的計算與記憶體需求對於實務部署至關重要。本節分析時間與空間複雜度，並討論可擴展性策略。

**時間複雜度**：對單個異常事件窗口的前向傳播，複雜度為 O(L·(|E|·C_edge + |V|·C_node))，其中：
- L 為消息傳遞層數（通常 2-4 層）；
- |E| 為邊數，|V| 為節點數；
- C_edge 為邊訊息生成的計算成本，取決於 KAN 基底數 K 與表徵維度 d（通常 O(K·d)）；
- C_node 為節點更新的成本，同樣與 K、d 相關。

對於典型微服務系統（|V| ~ 10-100，|E| ~ 20-500，L=3，K=5-10，d=64-128），單次前向傳播在毫秒級，滿足線上推論需求。訓練時，反向傳播成本約為前向的 2-3 倍，批次化處理多事件可充分利用 GPU 並行。

**空間複雜度**：記憶體主要由以下部分構成：
- **節點表徵**：O(L·|V|·d)，儲存各層的節點嵌入；
- **邊訊息**：O(|E|·d)，可按需計算以節省記憶體；
- **模型參數**：包括 GNN 權重（O(L·d²)）、邊權（O(|E|)）、KAN 係數（O(K·N_KAN)，N_KAN 為 KAN 模組數）。

實務中，模型參數量控制在數十萬至百萬級，可在單 GPU 上訓練與推論。

**可擴展性策略**：
- **批次化窗口**：訓練時批次處理多個事件，每事件對應一個子圖實例，利用 GPU 並行加速；
- **子圖抽樣**：對大規模系統（|V| > 1000），可採用鄰居採樣（只保留每節點的 top-K 鄰居）或子圖抽樣（只對異常相關的節點與邊建圖），降低計算成本且保留關鍵結構；
- **KAN 基底共享**：不同 KAN 模組可共享基底函數集合，只學習不同的係數，大幅減少參數量；
- **分散式推論**：在超大規模場景，可將圖分割並分散計算，最後彙整結果。

GNN_KAN 的設計兼顧表達力與效率，使其能部署於生產環境並支持即時根因分析。

---

## 7. 實驗 (Experiments)

全面之實證評估對於驗證 GNN_KAN 之有效性至關重要。本節描述實驗之資料集、評估指標、基線方法與實驗設定，確保評估之公平性與可重現性。

### 7.1 資料集

本研究於多個真實與合成之微服務基準架構上進行評估，涵蓋不同規模、拓樸與異常型態：

**Online Boutique**：Google 開源之電商微服務示範應用，包含約 10-15 個服務（前端、產品目錄、購物車、支付、推薦等），採用典型之扇出型拓樸。本研究注入延遲瓶頸（資料庫慢查詢）、資源耗盡（記憶體洩漏）、錯誤率飆升（外部 API 故障）等異常，產生數百個標註之根因窗口。

**Sock Shop**：WeaveWorks 之微服務基準，包含約 10 個服務（前端、目錄、購物車、訂單、支付、用戶管理等），拓樸結構較線性。注入異常包括 CPU 瓶頸、網路延遲、服務崩潰等，測試模型對不同故障模式之識別能力。

**Train Ticket**：複雜之火車票訂票系統，包含 40+ 個微服務，拓樸深度與廣度較大，依賴關係錯綜複雜。此資料集挑戰模型於大規模場景之可擴展性與多跳傳播推理能力。

**合成資料集**：為測試特定場景（如多根因、動態拓樸變化、極端雜訊），本研究生成合成資料集，可控制拓樸、異常型態、傳播延遲等參數，進行消融與敏感度分析。

所有資料集包含正常運行期之基線資料（用於特徵正規化）與異常事件窗口（帶根因標註）。資料切分為訓練集（60%）、驗證集（20%）、測試集（20%），確保事件間之時間獨立性以避免洩漏。

### 7.2 評估指標 (Metrics)

RCA 之核心目標為於候選清單前列找到根因，因此本研究採用排名導向之指標：

- **Top-k Accuracy（Acc@k，k=1,3,5）**：根因節點是否出現在預測排名之前 k 位。此為最直觀之指標，反映工程師檢查前 k 個候選能否找到根因之機率。
- **Precision@k / Recall@k**：前 k 個預測中正確根因之比例（Precision）與覆蓋全部根因之比例（Recall）。對於多根因場景，此類指標更為全面。
- **Mean Average Precision (mAP)**：平均所有事件之平均精度，考量整個排名清單之品質，對排名順序更為敏感。
- **Mean Reciprocal Rank (MRR)**：真實根因於排名中之倒數排名平均，反映根因之平均排名位置。

此外，本研究評估穩健性與可解釋性：
- **Robustness under Distribution Shift**：於負載變化（高低峰流量）、拓樸變更（服務增減）、異常型態偏移（訓練未見過之故障模式）下之性能衰減程度；
- **Explanation Consistency**：透過人工檢視與結構驗證，評估學習到之邊權與 KAN 函數是否與預期傳播路徑與領域知識一致。

### 7.3 基線方法 (Baselines)

為證明 GNN_KAN 之優勢，本研究與以下幾類代表性基線比較：

**統計與關聯方法**：
- **Pearson Correlation**：計算各節點指標與系統異常的相關性，按相關性排名；
- **Granger Causality**：基於時序的因果推論，識別領先於系統異常的節點；
- **Random Walk (PageRank)**：以異常分數為初始，在依賴圖上隨機遊走並排名。

**結構與路徑啟發法**：
- **Path-based Scoring**：追蹤異常呼叫鏈，按路徑長度與異常累積分數排名；
- **Rule-based RCA**：基於專家規則與閾值檢測的傳統方法。

**深度學習方法**：
- **GNN without KAN**：與 GNN_KAN 架構相同但使用標準啟動函數（ReLU），測試 KAN 的貢獻；
- **GNN with Full Features**：不遵循 RCA-aware 原則，使用所有可能特徵（包含洩漏特徵），測試特徵設計的重要性；
- **Node-wise Classifier (XGBoost/MLP)**：對每個節點獨立建模，不考慮圖結構。

### 7.4 實驗設定 (Setup)

**統一協定**：所有方法使用相同之資料切分、窗口長度（異常觸發前後各 5 分鐘）、特徵萃取流程（除特意測試特徵設計影響之消融實驗）。圖結構由服務依賴與調用追蹤資訊萃取，邊權初始化為正規化之呼叫頻率。

**超參數**：GNN_KAN 之超參數（層數 L=3、表徵維度 d=64、KAN 基底數 K=8、學習率 0.001、正則係數 λ₁=0.01, λ₂=0.001）透過驗證集調整。基線方法之超參數同樣透過驗證集優化，確保公平比較。

**計算環境**：實驗於配備 NVIDIA V100 GPU 之伺服器上執行，訓練與推論時間均記錄以評估效率。

**可重現性（RCAEval 平台）**：為使上述方法能於不同資料集與場景中以一致方式執行與比較，本研究將整體流程編排於 RCAEval 平台，包含標準化之資料載入（Datasets/Registry）、RCA-aware 特徵處理（Features/Processors）、雙圖構建（Graphs/Builders）與模型訓練評估（Runners/Evaluators）。所有實驗透過此平台執行，採用以下重現協定：
- **設定管理**：以平台之設定檔（configs）統一管理資料集、特徵處理、圖構建與模型超參數，所有實驗均附設定快照；
- **資料載入與版本控管**：資料集透過平台之 Datasets/Registry 統一載入，明確標示版本與切分，避免資料漂移；
- **隨機性控制**：於資料抽樣、權重初始化與運算後端層面固定隨機種子，確保多次執行結果一致（允許浮點微幅差異）；
- **實驗執行器**：使用平台 Runner 統一啟動訓練與評估，記錄指標、模型權重、圖與特徵摘要；
- **產物追蹤**：平台自動輸出實驗日誌、評估指標（CSV/JSON）、Top-k 排名清單、可視化圖（如 KAN 函數曲線、邊權熱圖），並以結構化目錄保存，利於審計與對比。

透過 RCAEval，本研究將方法論上之抽象（第 3.3–3.6 節）與實驗設計（本節）於平台層面緊密對齊，材料與設定可直接重用與重現，使他人可於相同或新資料上直接複現或延伸本研究之結果。

透過以上全面設計，本研究能客觀評估 GNN_KAN 於準確度、穩健性、可解釋性與效率上之表現，並與強基線進行對比。

---

## 8. 實驗結果與分析 (Results and Analysis)

本節報告 GNN_KAN 與基線方法 BARO 之實驗結果，涵蓋五個核心資料集（sock-shop-1、sock-shop-2、RE1、RE2、RE3）之全面評估。本研究比較 GNN_KAN 於不同基底函數（Chebyshev、B-spline、Fourier、PQC_GPU）下之表現，並與強基線 BARO 進行對比，深入分析各方法於不同故障類型與系統規模下之優劣。

### 8.1 整體性能比較 (Overall Performance Comparison)

#### 8.1.1 RE3 系列資料集：代碼級故障的挑戰

RE3 系列專注於代碼級故障（code-level faults），是評估精細根因定位能力的關鍵資料集。實驗結果顯示，GNN_KAN 在代碼級故障場景下展現顯著優勢：

**RE3-TT（Train Ticket 系統）**：
在 RE3-TT 資料集上，GNN_KAN 的所有基底函數變體（Chebyshev、B-spline、Fourier、PQC_GPU）均達到完美性能，所有指標（Precision@1、Precision@3、Precision@5、Avg@5）均為 1.00，展現了對複雜大規模系統代碼級故障的卓越識別能力。相較之下，BARO 基線在 Precision@1 上僅達到 0.7667，雖然 Precision@3 與 Precision@5 均為 1.00，但 Avg@5 為 0.9533，顯示其在 Top-1 根因定位上存在不足。此結果證實了 GNN_KAN 透過圖結構推理與 KAN 非線性轉換，能更精準地捕捉代碼級故障的細微異常信號。

**RE3-SS（Sock Shop 系統）**：
在 RE3-SS 資料集上，GNN_KAN 的表現同樣優於 BARO。BARO 在 Precision@1 上完全失敗（0.00），雖然 Precision@3 與 Precision@5 分別達到 0.9667 與 1.00，但 Avg@5 僅為 0.74，顯示其對代碼級故障的 Top-1 識別能力嚴重不足。GNN_KAN 的各基底函數變體表現優異：B-spline 達到最佳 Precision@1（0.8333）與 Avg@5（0.9667），Chebyshev、Fourier 與 PQC_GPU 的 Precision@1 均為 0.8，Avg@5 均為 0.96，且 Precision@3 與 Precision@5 均為 1.00。此結果表明，在中等規模系統的代碼級故障場景中，B-spline 基底函數最適合捕捉平滑的異常傳播模式。

**RE3-OB（Online Boutique 系統）**：
在 RE3-OB 資料集上，Chebyshev 基底函數表現最佳，Precision@1 達到 0.9667，Precision@3 與 Precision@5 均為 1.00，Avg@5 為 0.9933，接近完美性能。PQC_GPU 表現穩定（0.9/0.9/0.9/0.9），B-spline 與 Fourier 表現相近（0.8667/0.9/0.9/0.8933）。BARO 在 Precision@1 上再次失敗（0.00），Precision@3 與 Precision@5 為 0.9667，Avg@5 為 0.7667。此結果顯示，在扇出型拓樸的電商系統中，Chebyshev 基底函數的穩定逼近特性最適合代碼級故障的根因分析。

**RE3 系列綜合分析**：
RE3 系列的實驗結果揭示了代碼級故障根因分析的關鍵洞察：（1）GNN_KAN 相較於統計方法（BARO）在代碼級故障場景下具有顯著優勢，特別是在 Top-1 根因定位上；（2）不同基底函數在不同系統拓樸下表現各異，Chebyshev 適合扇出型拓樸，B-spline 適合線性拓樸，PQC_GPU 表現穩定但計算成本較高；（3）代碼級故障需要模型捕捉更微妙的異常信號，GNN_KAN 的圖結構推理能力使其能區分直接代碼錯誤與間接傳播效應。

#### 8.1.2 RE2 系列資料集：多源遙測數據的融合

RE2 系列包含多源遙測數據（metrics、logs、traces），規模與複雜度顯著高於 RE1，是評估模型在多源數據融合與大規模場景下性能的關鍵資料集。

**RE2-OB（Online Boutique 系統）**：
在 RE2-OB 資料集上，BARO 展現了統計方法在 Top-3 與 Top-5 根因定位上的優勢，Precision@3 與 Precision@5 分別達到 0.8778 與 0.9444，Avg@5 為 0.7422，但 Precision@1 僅為 0.1444，顯示其在精確根因定位上的不足。GNN_KAN 的各基底函數變體在 Precision@1 上表現優於 BARO：Chebyshev 與 Fourier 均為 0.4778，PQC 為 0.4778，B-spline 為 0.3667，但在 Precision@3 與 Precision@5 上略低於 BARO。Fourier 基底函數在 Avg@5 上表現最佳（0.76），Chebyshev 為 0.7578，顯示在大型多源數據場景中，Fourier 基底函數能有效捕捉週期性異常模式。

**RE2-SS（Sock Shop 系統）**：
在 RE2-SS 資料集上，BARO 再次展現了在 Top-3 與 Top-5 上的優勢（Precision@3: 0.8956, Precision@5: 0.9269, Avg@5: 0.7244），但 Precision@1 極低（0.0667）。GNN_KAN 的各基底函數在 Precision@1 上優於 BARO：Chebyshev 為 0.3333，Fourier 為 0.3222，B-spline 為 0.2889，PQC 為 0.2，但在 Precision@3 與 Precision@5 上略低於 BARO。此結果表明，在中等規模系統的多源數據場景中，GNN_KAN 與 BARO 各有優勢：GNN_KAN 更適合精確根因定位，BARO 更適合候選清單生成。

**RE2-TT（Train Ticket 系統）**：
在 RE2-TT 資料集上，BARO 展現了最佳整體性能，Precision@1 達到 0.5936，Precision@3 為 0.7572，Precision@5 為 0.7955，Avg@5 為 0.7152，在所有方法中領先。GNN_KAN 的各基底函數變體表現相近：PQC_GPU 的 Precision@1 最高（0.5222），Chebyshev 的 Precision@3 為 0.5909，但整體略低於 BARO。此結果顯示，在超大規模複雜系統（40+ 服務）的多源數據場景中，統計方法（BARO）可能因資料豐富度而獲得優勢，但 GNN_KAN 仍能在 Top-1 根因定位上提供競爭力。

**RE2 系列綜合分析**：
RE2 系列的實驗結果揭示了多源數據融合場景的複雜性：（1）BARO 在多源數據場景下展現了統計方法在候選清單生成上的優勢，特別是在 Top-3 與 Top-5 指標上；（2）GNN_KAN 在 Top-1 根因定位上仍具競爭力，顯示其圖結構推理能力在精確定位上的價值；（3）不同基底函數在多源數據場景下表現相近，Fourier 與 Chebyshev 略優，可能因其能捕捉多源數據中的週期性與穩定模式。

#### 8.1.3 RE1 系列資料集：標準化評估基準

RE1 系列是 RCAEval 平台的標準化評估資料集，包含 375 個故障案例，涵蓋資源故障與網路故障，是評估模型在標準場景下性能的關鍵基準。

**RE1-OB（Online Boutique 系統）**：
在 RE1-OB 資料集上，BARO 展現了強勁的整體性能，Precision@1 達到 0.65，Precision@3 為 0.896，Precision@5 為 0.928，Avg@5 為 0.784，在所有方法中領先。GNN_KAN 的各基底函數變體表現相近但略低於 BARO：Chebyshev 與 Fourier 的 Precision@1 均為 0.432，Precision@3 均為 0.624，Precision@5 均為 0.632，Avg@5 分別為 0.584 與 0.5856。B-spline 與 PQC 表現略低。此結果顯示，在標準化的資源與網路故障場景中，統計方法（BARO）可能因資料充分性而獲得優勢，但 GNN_KAN 仍能提供穩定的性能。

#### 8.1.4 基礎資料集：多樣化場景驗證

**Train Ticket 系統**：
於 Train Ticket 基礎資料集上，PQC_GPU 基底函數表現最佳，Precision@1 達到 0.256，Precision@3 為 0.376，Precision@5 為 0.456，Avg@5 為 0.3648，顯著優於其他基底函數變體與 BARO 基線，展現參數化量子電路於複雜系統中之優勢。BARO 表現第二（0.136/0.28/0.392/0.272），GNN_KAN 之其他基底函數變體（Chebyshev、B-spline、Fourier）表現較低。此結果明確表明，於超大規模複雜系統（40+ 服務）中，PQC_GPU 基底函數之指數級表示能力能有效捕捉深度非線性傳播模式，為此類場景之最佳選擇。

**Sock Shop-2 系統**：
在 Sock Shop-2 資料集上，PQC 與 Chebyshev 表現最佳，PQC 的 Precision@3 與 Precision@5 分別達到 0.792 與 0.952，Avg@5 為 0.6880；Chebyshev 的 Precision@5 為 0.936，Avg@5 為 0.6784。BARO 在 Precision@1 上略優（0.28），但整體表現相近。此結果顯示，在中等規模系統中，PQC 與 Chebyshev 基底函數均能提供穩定的性能。

### 8.2 故障類型分析 (Fault Type Analysis)

為深入理解各方法於不同故障類型下之表現，本研究分析 RE1-OB 資料集上各故障類型之詳細結果（見「RE1-OB 各故障類型性能比較表」之圖片）：

**資源故障（CPU、記憶體、磁碟）**：
在資源故障場景中，GNN_KAN 的 Chebyshev 基底函數展現了卓越性能：CPU 與記憶體故障的 Precision@1/3/5 均為 1.00，Avg@5 均為 1.00，達到完美識別。磁碟故障的 Precision@1 為 0.64，Precision@3 與 Precision@5 均為 1.00，Avg@5 為 0.92。BARO 在 CPU 與記憶體故障上同樣達到完美性能，但在磁碟故障的 Precision@1 上略低（0.72）。此結果表明，資源故障的異常信號較為明顯，GNN_KAN 與 BARO 均能有效識別，但 GNN_KAN 的 Chebyshev 基底函數在磁碟故障上略優。

**網路故障（延遲、封包遺失）**：
在網路故障場景中，BARO 展現了明顯優勢：延遲故障的 Precision@1 為 0.64，Precision@3 與 Precision@5 均為 1.00，Avg@5 為 0.92；封包遺失故障的 Precision@1 為 0.44，Precision@3 為 0.64，Precision@5 為 0.92，Avg@5 為 0.648。GNN_KAN 的各基底函數變體在網路故障上表現較差：延遲故障的 Precision@1 僅為 0.08，Precision@3 與 Precision@5 均為 0.28，Avg@5 為 0.208；封包遺失故障的 Precision@1 為 0.12，Precision@3 與 Precision@5 均為 0.52 或 0.48，Avg@5 為 0.424 或 0.392。此結果顯示，網路故障的異常傳播模式可能更適合統計方法捕捉，GNN_KAN 在網路故障場景下需要進一步優化。

**Socket 故障**：
所有方法在 Socket 故障上均完全失敗（所有指標為 0.00），顯示 Socket 故障的異常信號極其微弱或與其他故障類型混淆，需要更專門的特徵設計或故障注入策略。

### 8.3 基底函數比較與選擇策略 (Basis Function Comparison and Selection Strategy)

#### 8.3.1 各基底函數的特性分析

**Chebyshev 基底函數**：
Chebyshev 基底函數在代碼級故障（RE3 系列）與資源故障（RE1-OB）場景下表現最佳，特別是在 RE3-OB 與 RE1-OB 的 CPU/記憶體故障上達到完美性能。Chebyshev 多項式的穩定逼近特性使其適合捕捉平滑的異常傳播模式，在扇出型拓樸系統中表現優異。然而，在網路故障場景下表現較差，可能因其無法有效捕捉網路延遲的非線性模式。

**B-spline 基底函數**：
B-spline 基底函數在 RE3-SS（線性拓樸）上表現最佳，展現了分段平滑函數在線性依賴結構中的優勢。B-spline 的局部支撐性使其能建模分段平滑的非線性，適合捕捉局部異常傳播模式。但在多源數據場景（RE2 系列）中表現略低，可能因其對全局模式的捕捉能力有限。

**Fourier 基底函數**：
Fourier 基底函數在多源數據場景（RE2-OB）的 Avg@5 指標上表現最佳，展現了週期性基底在捕捉負載相關異常模式上的優勢。Fourier 基底適合建模週期性模式，對負載相關的週期性異常特別有效。但在代碼級故障場景下表現略低於 Chebyshev，可能因其對非週期性異常的捕捉能力有限。

**PQC_GPU 基底函數**：
PQC_GPU 基底函數在超大規模複雜系統（Train Ticket、RE2-TT）上展現優勢，展現了參數化量子電路的指數級表示能力。PQC_GPU 特別適合捕捉複雜的多服務協同故障、高維特徵異常與深度非線性傳播模式。然而，其計算成本較高，在中等規模系統中可能不如 Chebyshev 或 B-spline 高效。

#### 8.3.2 基底函數選擇策略

基於上述實驗結果，本研究提出以下基底函數選擇策略：

**策略一：基於系統拓樸結構選擇**
- **扇出型拓樸**（如 Online Boutique）：優先選擇 **Chebyshev** 基底函數，因其穩定逼近特性適合多服務依賴場景。
- **線性拓樸**（如 Sock Shop）：優先選擇 **B-spline** 基底函數，因其分段平滑特性適合線性依賴結構。
- **深層複雜拓樸**（如 Train Ticket，40+ 服務）：優先選擇 **PQC_GPU** 基底函數，實驗結果顯示其於 Train Ticket 資料集上表現最佳（Precision@1=0.256，Avg@5=0.3648），其指數級表示能力適合複雜多跳傳播場景。

**策略二：基於故障類型選擇**
- **資源故障**（CPU、記憶體、磁碟）：優先選擇 **Chebyshev** 基底函數，實驗顯示其在資源故障上達到最佳性能。
- **網路故障**（延遲、封包遺失）：目前所有 GNN_KAN 基底函數表現均不理想，建議使用 **BARO** 統計方法或進一步優化特徵設計。
- **代碼級故障**：根據系統規模選擇，中等規模選擇 **B-spline**，大規模選擇 **Chebyshev** 或 **PQC_GPU**。

**策略三：基於資料豐富度選擇**
- **純指標數據**（RE1 系列）：優先選擇 **Chebyshev** 或 **BARO**，統計方法在資料充分時可能獲得優勢。
- **多源數據**（RE2 系列，包含 logs、traces）：優先選擇 **Fourier** 基底函數，因其能捕捉多源數據中的週期性模式。
- **超大規模數據**（RE2-TT、Train Ticket）：優先選擇 **PQC_GPU** 基底函數，因其指數級表示能力適合高維複雜場景。

**策略四：基於性能與效率權衡**
- **追求最佳性能**：根據上述策略選擇最適合的基底函數。
- **追求計算效率**：優先選擇 **Chebyshev** 或 **B-spline**，計算成本較低。
- **平衡性能與效率**：選擇 **Chebyshev** 作為預設基底函數，在大多數場景下提供穩定性能。

### 8.4 與基線方法的綜合比較 (Comprehensive Comparison with Baselines)

#### 8.4.1 GNN_KAN vs BARO：優勢與劣勢分析

**GNN_KAN 的優勢**：
1. **代碼級故障場景**：在 RE3 系列資料集上，GNN_KAN 的所有基底函數變體均顯著優於 BARO，特別是在 Top-1 根因定位上達到完美或接近完美性能，展現了圖結構推理在精細故障識別上的優勢。
2. **資源故障的 Top-1 定位**：在 RE1-OB 的 CPU/記憶體故障上，GNN_KAN 的 Chebyshev 基底函數達到完美性能，優於 BARO。
3. **可解釋性**：GNN_KAN 透過 KAN 基底函數的可視化與邊權分析，提供更豐富的可解釋性，而 BARO 作為統計方法，可解釋性相對有限。

**BARO 的優勢**：
1. **網路故障場景**：在 RE1-OB 的延遲與封包遺失故障上，BARO 顯著優於 GNN_KAN 的所有基底函數變體，展現了統計方法在網路異常模式捕捉上的優勢。
2. **多源數據的候選清單生成**：在 RE2 系列資料集上，BARO 在 Top-3 與 Top-5 指標上優於 GNN_KAN，顯示其在候選清單生成上的優勢。
3. **超大規模系統**：在 RE2-TT 與 Train Ticket 等超大規模系統中，BARO 的整體性能優於 GNN_KAN，可能因其對資料豐富度的利用更充分。

**綜合評估**：
GNN_KAN 與 BARO 各有優勢，適用於不同場景。GNN_KAN 更適合代碼級故障與精確根因定位，BARO 更適合網路故障與候選清單生成。在實務部署中，建議根據故障類型與系統特性選擇最適合的方法，或採用集成策略結合兩者優勢。

### 8.5 可視化結果 (Visualization Results)

為直觀展示實驗結果，我們提供了多個資料集的雷達圖（見「各資料集整體性能雷達圖」的圖片），包含：
- **RE3 系列雷達圖**：展示 GNN_KAN 各基底函數在代碼級故障上的卓越表現。
- **RE2 系列雷達圖**：展示多源數據場景下各方法的性能分佈。
- **RE1 系列雷達圖**：展示標準化評估基準下的方法比較。
- **基礎資料集雷達圖**：展示多樣化場景下的方法表現。

這些可視化結果清晰地展示了各方法在不同指標維度上的優勢與劣勢，為方法選擇提供直觀參考。

### 8.6 實驗結果總結 (Summary of Experimental Results)

綜合所有實驗結果，我們得出以下關鍵結論：

1. **GNN_KAN 在代碼級故障場景下具有顯著優勢**：在 RE3 系列資料集上，GNN_KAN 的所有基底函數變體均達到完美或接近完美性能，顯著優於 BARO 基線。

2. **不同基底函數適用於不同場景**：Chebyshev 適合扇出型拓樸與資源故障，B-spline 適合線性拓樸，Fourier 適合多源數據，PQC_GPU 適合超大規模複雜系統。

3. **BARO 在網路故障與候選清單生成上具有優勢**：統計方法在網路異常模式捕捉與多源數據的候選清單生成上表現優異。

4. **方法選擇應基於系統特性與故障類型**：實務部署中應根據系統拓樸、故障類型、資料豐富度與性能效率權衡選擇最適合的方法或基底函數。

這些實驗結果為 GNN_KAN 的實務應用提供了明確的指導原則，並為未來研究指明了優化方向。

---

## 9. 討論 (Discussion)

實驗結果證實 GNN_KAN 之有效性，然更重要者為理解「為何有效」與「於何種情境下有效」。本節深入討論方法之關鍵洞察與適用性。

### 9.1 為何 KAN 有助於 RCA？ (Why KAN Helps RCA?)

**結構化函數分解降低過度擬合**：傳統黑箱非線性（如 MLP 搭配 ReLU）允許模型學習任意高維非線性映射，此於資料充足時為優勢，然於 RCA 場景中訓練資料往往有限（異常事件相對罕見），且分布可能與部署時不同。Kolmogorov-Arnold Networks（KAN）透過一維函數分解限制函數空間之結構，降低有效參數維度，使模型於有限資料下更穩健。實驗中，KAN 於罕見異常型態與跨場景測試上之優勢即源於此結構化先驗。

**一維函數可視化增強信任與除錯能力**：KAN 將多變量映射分解為獨立之一維函數 $\psi_i(x_i)$，每個函數可單獨繪製與檢視。研究者與工程師能驗證模型是否學習到合理之傳播邏輯：若某邊 KAN 之一維函數呈現反常之下降曲線（異常越強、傳播越弱），可即時發現並修正（如檢查特徵或資料問題）。此一維函數級別之透明性於高維 MLP 中不可得，對於部署信任至關重要。

**基底函數選擇與領域知識注入**：透過選擇適合 RCA 之一維基底函數（如 B-spline 用於平滑傳播曲線、Chebyshev 多項式用於穩定逼近、Piecewise Linear 用於門檻建模），本研究將領域期望編碼至模型結構中，使其於少量標註下亦能學習合理模式。此類似 Physics-Informed Neural Networks 將物理方程注入深度學習，然 KAN 之 Kolmogorov-Arnold 分解更通用，無需顯式物理方程即可提供結構化歸納偏置。

### 9.2 對資料需求的影響 (Impact on Data Requirements)

RCA 標註成本高昂（需人工確認根因），因此方法之樣本效率至關重要。本研究觀察到，GNN_KAN 於訓練資料減半（30% 訓練集）時，性能僅下降 5-8%，而 GNN without KAN 下降達 15-20%。此歸因於 KAN 之先驗約束使模型於少量樣本下亦能學習到穩健之模式，而非過度擬合雜訊。此優勢使 GNN_KAN 更適合實務中標註稀缺之場景。

### 9.3 與因果方法的互補性 (Complementarity with Causal Methods)

因果推論方法（如因果發現、結構方程模型）試圖重建真實的因果圖，理論上更具解釋力。然而，在高頻、高維、非平穩的運維環境中，因果方法面臨計算成本、假設違反（如因果充分性、平穩性）與樣本需求的挑戰。GNN_KAN 並非替代因果方法，而是提供互補的解決方案：

- **結構不確定下之快速推理**：當因果圖不確定或部分可得時，GNN_KAN 可基於觀測與依賴圖進行近似推理，速度快且對假設要求低；
- **因果圖作為先驗**：若已有因果發現之結果（或部分先驗知識），可將其作為 GNN 之邊權初始化或結構約束，結合資料驅動學習與先驗知識；
- **混合架構**：未來可探索將因果推論與 GNN_KAN 結合，例如先以因果發現識別關鍵依賴，再以 GNN_KAN 精細化根因排名。

### 9.4 可擴展至其他 AIOps 任務 (Extensibility to Other AIOps Tasks)

GNN_KAN 之核心思想——圖結構推理 + 可解釋非線性——不限於 RCA，可擴展至其他 AIOps 任務：
- **異常偵測**：於圖上識別異常節點或子圖；
- **故障預測**：預測未來可能發生故障之節點；
- **影響分析**：評估某服務變更或故障對系統之影響範圍；
- **容量規劃**：基於依賴與負載分析資源瓶頸。

此類任務均涉及結構化推理與可解釋性需求，GNN_KAN 提供通用之框架與工具。

---

## 10. 威脅與限制 (Threats to Validity)

誠實評估方法之局限性對科學研究至關重要。本節討論 GNN_KAN 面臨之威脅與限制，並提出緩解策略。

### 10.1 內部效度威脅 (Internal Validity Threats)

**拓樸不完整與不準確**：GNN_KAN 依賴服務依賴圖作為結構先驗，然實務中圖可能不完整（追蹤資料遺漏部分呼叫，如跨網路非同步調用）或不準確（動態路由、第三方服務）。此可能導致模型無法捕捉實際之傳播路徑。緩解策略包括：（1）結合多源資訊（配置檔、追蹤、網路拓樸）構建更完整之圖；（2）以學習式邊權自適應修正圖結構；（3）引入不確定性量化，對圖不確定區域之預測給予信心度估計。

**標註偏差與雜訊**：根因標註通常由人工事後分析產生，可能包含錯誤或主觀判斷（例如將症狀誤標為根因）。標註雜訊會影響訓練信號並誤導模型。緩解策略包括：（1）引入標註信心度權重，對不確定標註降低損失貢獻；（2）使用半監督或自監督方法減少對標註之依賴；（3）透過多輪標註與專家共識提升標註品質。

### 10.2 外部效度威脅 (External Validity Threats)

**資料集代表性**：實驗使用之微服務基準（Online Boutique、Sock Shop、Train Ticket）雖具代表性，然可能無法涵蓋所有實務場景（如超大規模系統、特定領域應用）。未來需於更多樣化之真實生產環境中驗證。

**異常型態偏差**：訓練資料中之異常型態（延遲、錯誤率、資源瓶頸）可能與部署時遇到之新型態不同（如零日漏洞、惡意攻擊）。雖 KAN 提升對未見模式之泛化能力，然極端新穎之故障模式仍可能挑戰模型。緩解策略包括持續學習與線上適應，以新觀測到之異常事件更新模型。

### 10.3 建構效度威脅 (Construct Validity Threats)

**評估指標之適切性**：Top-k Accuracy 與 mAP 雖為標準排名指標，然未必完全反映實務效用（如工程師實際使用時之認知負擔、診斷時間節省）。未來研究可引入用戶研究與現場實驗，評估模型對實際運維效率之影響。

**可解釋性之量化**：目前對可解釋性之評估主要透過人工檢視與案例研究，缺乏標準化之量化指標。未來可開發可解釋性度量（如與專家知識之一致性分數、路徑合理性評分），使評估更客觀。

### 10.4 結論效度威脅 (Conclusion Validity Threats)

**超參數敏感性**：模型性能可能對超參數（如層數、基底數、正則係數）敏感，不當設置可能導致次優結果。本研究透過驗證集調整超參數並進行敏感度分析，然無法保證於所有場景下之最優性。實務部署時需針對具體系統進行調整。

**極端分布偏移**：雖 GNN_KAN 於跨場景測試中表現穩健，然於極端分布偏移（如系統架構徹底重構、負載模式劇變）下仍可能性能下降。緩解策略包括：（1）定期以新資料重訓或微調模型；（2）引入分布監控，當檢測到顯著偏移時觸發模型更新；（3）結合規則方法作為備援，確保極端情況下之基本可用性。

總體而言，GNN_KAN 於當前實驗設定下展現顯著優勢，然實務部署需考量上述威脅並採取相應緩解措施，確保長期穩健與可信。

---

## 11. 結論與未來工作 (Conclusion and Future Work)

### 11.1 研究總結 (Summary)

本研究針對現代分散式系統根因分析面臨之挑戰——異常傳播掩蔽、高維雜訊、動態拓樸、可解釋性需求——提出 GNN_KAN，一結合圖神經網路與 Kolmogorov-Arnold Networks 之創新框架。GNN_KAN 之核心貢獻如下：

首先，本研究將 Kolmogorov-Arnold Networks（KAN）引入 GNN 之消息傳遞機制，利用 Kolmogorov-Arnold 表徵定理將多變量非線性映射分解為一維可學習函數之組合，取代傳統之黑箱 MLP。透過選擇適當之一維基底函數（如 B-spline、Chebyshev 多項式、Fourier 基底、PQC_GPU），KAN 於提供強大逼近能力之同時，降低參數複雜度並提升可解釋性。一維函數可獨立視覺化與驗證，使工程師能理解模型如何建模異常傳播之非線性模式（如單調性、門檻性、飽和性）。

其次，本研究系統化地定義 RCA-aware 特徵設計原則，確保特徵與根因定位任務高度相關且於部署中可行。透過排除未來資訊、標註耦合與後驗指標，並採用一致之正規化與對齊策略，本研究使模型學習到通用之異常傳播模式，而非訓練集特定之偽相關，大幅提升跨場景穩健性。

第三，本研究於五個核心資料集（sock-shop-1、sock-shop-2、RE1、RE2、RE3）上進行全面評估，涵蓋從中等規模到超大規模、從資源/網路故障到代碼級故障之完整評估光譜。實驗結果顯示，GNN_KAN 於代碼級故障場景（RE3 系列）下展現顯著優勢，所有基底函數變體均達到完美或接近完美性能，顯著優於統計基線 BARO。於資源故障場景中，GNN_KAN 之 Chebyshev 基底函數達到完美性能。特別值得注意的是，於超大規模複雜系統（Train Ticket）中，PQC_GPU 基底函數表現最佳（Precision@1=0.256，Avg@5=0.3648），顯著優於其他基底函數與 BARO 基線，展現參數化量子電路於複雜多跳傳播場景中之優勢。然於網路故障與部分多源數據場景中，BARO 展現統計方法之優勢，顯示不同方法適用於不同場景。

第四，基於實驗結果，本研究提出系統化之基底函數選擇策略，根據系統拓樸結構、故障類型、資料豐富度與性能效率權衡，為實務部署提供明確之指導原則。此選擇策略使 GNN_KAN 能根據具體場景選擇最適合之基底函數，最大化根因分析之準確度與效率。

#### 11.1.1 基底函數選擇的實務指導原則 (Practical Guidelines for Basis Function Selection)

基於五個核心資料集（sock-shop-1、sock-shop-2、RE1、RE2、RE3）之實驗結果，本研究提出以下實務導向之基底函數選擇方法：

**方法一：基於資料集類型之快速選擇法**

依據實驗結果，本研究可根據目標系統與資料集特性快速選擇基底函數：

1. **代碼級故障場景（對應 RE3 系列資料集）**：
   - **扇出型拓樸系統**（如 Online Boutique）：優先選擇 **Chebyshev** 基底函數。實驗顯示，於 RE3-OB 資料集上，Chebyshev 達到 Precision@1=0.9667、Avg@5=0.9933 之卓越性能，接近完美識別。
   - **線性拓樸系統**（如 Sock Shop）：優先選擇 **B-spline** 基底函數。實驗顯示，於 RE3-SS 資料集上，B-spline 達到 Precision@1=0.8333、Avg@5=0.9667 之最佳性能。
   - **超大規模複雜系統**（如 Train Ticket，40+ 服務）：優先選擇 **PQC_GPU** 基底函數。實驗結果明確顯示，於 Train Ticket 基礎資料集上，PQC_GPU 表現最佳（Precision@1=0.256，Precision@3=0.376，Precision@5=0.456，Avg@5=0.3648），顯著優於其他基底函數與 BARO 基線，其指數級表示能力適合複雜多跳傳播場景。

2. **多源遙測數據場景（對應 RE2 系列資料集）**：
   - **大型多源數據系統**（如 RE2-OB）：優先選擇 **Fourier** 基底函數。實驗顯示，Fourier 在 RE2-OB 的 Avg@5 指標上表現最佳（0.76），能有效捕捉多源數據中的週期性異常模式。
   - **超大規模多源數據系統**（如 RE2-TT）：建議使用 **BARO** 統計方法，或選擇 **PQC_GPU** 基底函數。實驗顯示，在 RE2-TT 上，BARO 的整體性能優於 GNN_KAN，但 GNN_KAN 的 PQC_GPU 在 Top-1 根因定位上仍具競爭力。

3. **標準化資源/網路故障場景（對應 RE1 系列資料集）**：
   - **資源故障**（CPU、記憶體、磁碟）：優先選擇 **Chebyshev** 基底函數。實驗顯示，在 RE1-OB 的 CPU/記憶體故障上，Chebyshev 達到完美性能（Precision@1/3/5=1.00）。
   - **網路故障**（延遲、封包遺失）：建議使用 **BARO** 統計方法。實驗顯示，GNN_KAN 的所有基底函數在網路故障上表現均不理想，而 BARO 展現明顯優勢。

**方法二：基於性能指標優先級之選擇法**

依據實務需求，優先考慮不同性能指標：

1. **優先考慮 Top-1 根因定位準確度**（Precision@1）：
   - 於代碼級故障場景（RE3 系列）中，選擇 **Chebyshev**（RE3-OB）或 **B-spline**（RE3-SS）。
   - 於多源數據場景（RE2 系列）中，選擇 **Chebyshev** 或 **Fourier**（Precision@1 約 0.48，優於 BARO 之 0.14-0.15）。
   - 於資源故障場景中，選擇 **Chebyshev**（達到完美性能）。
   - **特別注意**：於超大規模複雜系統（Train Ticket）中，**PQC_GPU** 於 Precision@1 上表現最佳（0.256），顯著優於其他方法。

2. **優先考慮候選清單品質**（Precision@3/5、Avg@5）：
   - 於多源數據場景中，選擇 **BARO** 統計方法（於 RE2 系列上展現優勢）。
   - 於代碼級故障場景中，所有 GNN_KAN 基底函數均表現優異，可選擇 **Chebyshev** 作為預設。

**方法三：基於計算資源與效率之選擇法**

依據部署環境之計算資源限制：

1. **計算資源充足**：
   - 優先選擇 **PQC_GPU** 基底函數，特別於超大規模複雜系統（如 Train Ticket，40+ 服務）中，能獲得指數級表示能力與最佳性能。實驗結果明確顯示，PQC_GPU 於 Train Ticket 基礎資料集上表現最佳。

2. **計算資源有限**：
   - 優先選擇 **Chebyshev** 或 **B-spline** 基底函數，計算成本較低，且於大多數場景下提供穩定性能。

3. **平衡性能與效率**：
   - 選擇 **Chebyshev** 作為預設基底函數，於大多數場景下提供穩定且高效之性能。

**方法四：混合策略與集成方法**

於實務部署中，可採用以下混合策略：

1. **故障類型分流**：
   - 資源故障與代碼級故障：使用 GNN_KAN（Chebyshev 或 B-spline）。
   - 網路故障：使用 BARO 統計方法。
   - **超大規模複雜系統**：優先使用 **PQC_GPU** 基底函數。

2. **兩階段策略**：
   - 第一階段：使用 BARO 生成候選清單（Top-5）。
   - 第二階段：使用 GNN_KAN（Chebyshev 或 PQC_GPU，依系統規模而定）對候選清單進行精細化排序，提升 Top-1 準確度。

3. **集成預測**：
   - 同時運行多個基底函數（Chebyshev、B-spline、Fourier、PQC_GPU），透過投票或加權平均整合預測結果，提升穩健性。

**實務部署建議**：

基於上述實驗結果與選擇策略，本研究建議實務部署採用以下流程：

1. **系統特性分析**：識別系統拓樸結構（扇出型/線性/深層複雜）、故障類型分佈（資源/網路/代碼級）、資料豐富度（純指標/多源數據）。
2. **基底函數選擇**：依據上述方法選擇最適合之基底函數或方法。**特別注意**：對於 Train Ticket 等超大規模複雜系統（40+ 服務），優先考慮 **PQC_GPU** 基底函數。
3. **驗證與調整**：於驗證集上評估選擇之基底函數，依據實際性能進行微調。
4. **持續監控**：部署後持續監控模型性能，當系統特性或故障模式發生變化時，重新評估基底函數選擇。

此系統化之選擇方法使 GNN_KAN 能根據具體場景最大化根因分析之準確度與效率，為實務部署提供明確且可操作之指導原則。

### 11.2 未來研究方向 (Future Directions)

雖 GNN_KAN 展現良好之性能與潛力，然仍有多個具建設性與架構性之方向值得深入探索：

#### 11.2.1 理論與方法論擴展

**因果推論與 GNN_KAN 之深度融合**：
目前 GNN_KAN 主要依賴統計相關性與結構先驗進行根因推論，未來可探索將因果發現方法（如 PC 演算法、FCI 演算法、結構方程模型）與 GNN_KAN 之深度融合架構。具體而言：
- **因果圖作為結構先驗**：以因果發現識別之真實因果邊作為 GNN 之初始邊權，並透過可學習之邊權調整機制於訓練中精細化，使模型同時具備因果嚴謹性與資料驅動之適應性；
- **混合推理架構**：設計分層架構，底層以因果推論識別高置信度之直接因果關係，上層以 GNN_KAN 處理間接傳播與複雜交互，透過注意力機制動態選擇推理路徑；
- **因果感知之 KAN 基底**：將因果結構（如中介變數、混淆變數）編碼至 KAN 基底函數之選擇與初始化，使一維函數之形狀反映因果機制（如單調性、門檻性、飽和性）。

**可解釋性之量化與標準化**：
目前對可解釋性之評估主要依賴人工檢視與案例研究，缺乏標準化之量化指標。未來可建立：
- **可解釋性度量框架**：定義多維度可解釋性指標，包括路徑合理性分數（學習到之傳播路徑與專家知識之一致性）、函數形狀合理性（KAN 一維函數是否符合領域預期）、決策透明度（根因分數之計算過程是否可追溯）；
- **可解釋性基準測試**：構建標準化之可解釋性評估資料集，包含已知傳播路徑之故障案例，用於客觀比較不同方法之可解釋性；
- **自動化可解釋性驗證**：開發工具自動檢查 KAN 函數曲線是否違反領域約束（如異常強度與傳播強度應單調相關），並提供修正建議。

#### 11.2.2 架構與系統層面優化 (Architectural and System-Level Optimizations)

**神經架構搜尋（NAS）驅動之 KAN 設計**：
目前 KAN 之基底函數類型（Chebyshev、B-spline、Fourier、PQC_GPU）與數量需人工選擇，未來可探索：
- **自動化基底選擇**：設計 NAS 搜尋空間，包含基底類型、基底數量、網格大小、樣條階數等超參數，以驗證集性能為目標自動搜尋最優配置；
- **服務特定之基底適配**：不同服務可能適合不同之基底函數（如資料庫服務適合 B-spline 建模平滑傳播，API 服務適合 Chebyshev 建模穩定逼近，超大規模複雜系統適合 PQC_GPU），設計元學習機制自動為每個服務選擇最適合之基底；
- **動態基底調整**：於訓練過程中根據梯度資訊動態調整基底函數之形狀與數量，實現自適應之函數逼近。

**多圖融合與異構圖學習**：
實務中微服務系統可能包含多種圖結構（服務依賴圖、資料流圖、網路拓樸圖、資源共享圖），未來可研究：
- **異構圖神經網路架構**：擴展 GNN_KAN 至異構圖（heterogeneous graph），設計不同類型之消息傳遞機制處理不同類型之邊（服務呼叫邊、資料流邊、資源共享邊），並以 KAN 融合多圖資訊；
- **多尺度圖學習**：同時建模局部圖結構（單一服務之鄰居）與全局圖結構（整個系統之拓樸），透過多尺度 KAN 模組捕捉不同層級之異常傳播模式；
- **時序圖神經網路**：將靜態依賴圖擴展為時序圖（temporal graph），捕捉依賴關係隨時間之動態變化，並以時序 KAN 建模時間維度之非線性模式。

#### 11.2.3 學習範式與適應機制 (Learning Paradigms and Adaptation Mechanisms)

**少樣本學習與遷移學習**：
RCA 標註成本高昂，新系統之標註資料往往稀缺，未來可研究：
- **跨系統遷移學習**：設計跨圖遷移學習框架，利用源系統（如 Online Boutique）之知識加速目標系統（如 Train Ticket）之學習，透過圖對齊（graph alignment）與特徵對齊（feature alignment）實現知識轉移；
- **元學習快速適應**：開發 MAML（Model-Agnostic Meta-Learning）或 Prototypical Networks 之變體，使 GNN_KAN 能於少量標註下快速適應新系統，透過元學習學習「如何學習根因模式」；
- **半監督與自監督學習**：利用大量未標註之異常事件，設計對比學習（contrastive learning）或圖重建（graph reconstruction）任務，從無標註資料中學習異常傳播之通用模式。

**持續學習與線上適應**：
分散式系統持續演化（服務增減、依賴變更、負載模式變化），模型需隨之更新，未來可研究：
- **增量學習機制**：設計不遺忘（non-forgetting）之增量學習策略，使模型於學習新系統時保留對舊系統之知識，避免災難性遺忘（catastrophic forgetting）；
- **線上適應框架**：開發線上學習機制，使模型能於部署後持續從新觀測之異常事件中學習，透過滑動窗口、重要性採樣與模型蒸餾實現高效之線上更新；
- **分布偏移偵測與適應**：設計分布偏移偵測器，當檢測到系統分布發生顯著變化時自動觸發模型重訓或微調，確保長期有效性。

#### 11.2.4 不確定性量化與決策支援 (Uncertainty Quantification and Decision Support)

**貝氏 GNN_KAN 與不確定性估計**：
為模型預測提供信心度估計，幫助工程師判斷何時信任模型、何時需人工介入，未來可研究：
- **貝氏 KAN**：將 KAN 之基底係數視為隨機變數，透過變分推論（variational inference）或 MCMC 採樣估計後驗分布，提供預測之不確定性區間；
- **集成學習與模型平均**：訓練多個 GNN_KAN 模型（不同初始化、不同基底函數），透過集成預測與投票機制提供信心度估計，並識別高不確定性之異常事件；
- **對抗性驗證**：設計對抗樣本生成器，測試模型於邊緣案例（edge cases）下之穩健性，並提供不確定性警告。

**主動學習與智慧標註**：
標註成本為 RCA 模型部署之關鍵瓶頸，未來可研究：
- **主動學習策略**：設計查詢策略（query strategy）選擇最有價值之異常事件進行標註，透過不確定性採樣（uncertainty sampling）、多樣性採樣（diversity sampling）或預期模型變更（expected model change）最大化標註效率；
- **弱監督學習**：利用弱標註（如「根因可能在服務 A、B、C 中」）或部分標註（如「服務 A 不是根因」）訓練模型，降低標註成本；
- **人機協作標註**：設計互動式標註介面，模型提供候選根因與支持證據，專家確認或修正，形成人機協作之標註循環。

#### 11.2.5 應用擴展與生態系統 (Application Extensions and Ecosystem)

**擴展至其他 AIOps 任務**：
GNN_KAN 之圖推理 + 可解釋非線性框架可應用於廣泛之 AIOps 任務，未來可研究：
- **異常偵測**：將根因分析擴展為異常偵測，於圖上識別異常節點或子圖，並以 KAN 提供異常程度之可解釋分數；
- **故障預測**：基於歷史故障模式與當前系統狀態，預測未來可能發生故障之服務，透過時序 GNN_KAN 建模故障演進模式；
- **影響分析**：評估某服務變更或故障對系統之影響範圍，透過圖傳播模擬與 KAN 量化影響強度；
- **容量規劃**：基於服務依賴與負載分析資源瓶頸，透過 GNN_KAN 預測資源需求與擴容建議。

**開源生態系統與工具鏈**：
為推動 GNN_KAN 之實務應用，未來可建立：
- **標準化 API 與 SDK**：提供統一之 Python API，支援主流微服務框架（Kubernetes、Istio、Prometheus）之資料接入，降低部署門檻；
- **可視化工具與儀表板**：開發互動式可視化工具，展示 KAN 函數曲線、傳播路徑、根因排名與不確定性，提供直觀之決策支援介面；
- **模型市場與預訓練模型**：建立模型市場，提供針對不同系統類型（電商、金融、物聯網）之預訓練 GNN_KAN 模型，支援快速部署與遷移；
- **基準測試與競賽平台**：擴展 RCAEval 平台，定期舉辦根因分析競賽，推動方法創新與性能提升。

透過上述未來研究方向，GNN_KAN 可從單一方法演進為完整之 AIOps 解決方案生態系統，推動智慧運維技術之理論發展與實務應用。

### 11.3 最終評論 (Final Remarks)

GNN_KAN 代表根因分析研究從黑箱模型向可解釋、可信任 AI 之轉變。透過 Kolmogorov-Arnold 表徵之結構化函數分解與 RCA-aware 之特徵設計，本研究展示將數學基礎理論（Kolmogorov-Arnold 表徵定理）與深度學習結合於實務問題之有效性。隨著雲端系統複雜度之持續增長，可解釋且穩健之 RCA 工具將成為保障服務可靠性之關鍵。本研究期待 GNN_KAN 能為學術界與產業界提供有價值之參考，並激發更多基於數學原理之可解釋 AI 於運維領域之創新。

---

## 附錄 A：實作與重現細節（文字概述）(Implementation & Reproducibility - Text Only)

本研究所有實驗均透過 RCAEval 平台執行，以確保方法論、資料處理與評估協定的可重現性與可審計性。完整之實驗架構（資料/圖管線、模型與學習、設定與執行流程、重現步驟）已整合至正文第 4 節「實驗架構」。為避免重複，此附錄僅作指引：請參見第 4 節取得詳細步驟與對應關係；第 3 節涵蓋方法論細節；第 7 節描述實驗設定與評估協定。

## 參考文獻（略）(References - omitted for draft)
