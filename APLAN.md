一、 研究計畫之背景及目的
1.1 研究背景與動機 (更詳細描述)
隨著雲端原生（Cloud Native）技術的普及，現代企業應用已從單體架構全面轉向微服務架構（Microservices Architecture）。雖然微服務提升了開發效率與系統彈性，但也帶來了前所未有的維運挑戰：
異常傳播的複雜性（Failure Propagation）：在高度解耦的環境中，服務間透過頻繁的 RPC 或 REST 調用相互依賴。單一基礎設施的故障（如 Disk I/O 延遲）會迅速沿著調用鏈向下游擴散，引發連鎖反應（Cascading Failures）。這種現象常導致下游服務的「症狀」比上游根因更為劇烈，產生所謂的「根因掩蓋」（Root Cause Masking）效應，使維運人員難以區分誰是真正的肇事者，誰是受害者。
數據多模態與語義鴻溝（Data Heterogeneity）：目前的觀測數據主要分為三類：指標（Metrics）、追蹤（Traces）與日誌（Logs）。指標反映了狀態但缺乏結構關係；追蹤提供了拓撲路徑但難以量化性能波動的因果強度。如何將這些異質數據有效融合，並在動態變化的拓撲中精確定位根因，是當前學術界與業界亟待解決的難題。
黑箱模型的信任危機（The Black-box Problem）：雖然深度學習模型（如 GNN）在故障偵測上表現優異，但其決策過程缺乏透明度。在關鍵維運場景中，工程師若無法理解模型為何判定某服務為根因，將不敢貿然執行自動化修復（Auto-remediation），這限制了 AIOps 邁向完全自治的最後一哩路。
1.2 相關背景科技介紹
圖神經網路（GNN）與拓撲感知：
GNN 具備處理非歐幾里得結構數據的能力，天然適合描述微服務的依賴圖（Dependency Graph）。透過訊息傳遞（Message Passing）機制，GNN 能捕捉節點間的拓撲特徵，是目前複雜系統表徵學習的主流技術。
Kolmogorov-Arnold Networks (KAN)：
這是 2024 年受數學定理啟發的新型架構。不同於傳統 MLP 將權重放在節點上，KAN 將可學習的活化函數（如 B-Splines）放在「邊」上。這使得模型能將複雜的多變量非線性關係分解為一維函數組合，具備極佳的數學解釋性與符號擬合能力，為解決 AIOps 的「黑箱」問題提供了新工具。
賽局理論歸因：Shapley Value：
源自合作賽局理論，Shapley Value 提供了一種公理化的方法，用於計算各參與者在合作中的邊際貢獻。在 RCA 場景中，可將各個服務節點視為參與者，將系統的異常程度視為收益，藉此公平且嚴謹地量化每個服務對「故障結果」的責任權重。
1.3 近期相關研究發展趨勢 (Trend Analysis)
根據 Top-tier 研討會（如 ICSE, FSE, ASE, CCGRID）近兩年的研究進展，微服務根因分析呈現以下趨勢：
從「統計相關」轉向「因果推論」（From Correlation to Causation）：
早期的研究（如 MicroHECL, Sage）多依賴皮爾森相關係數或簡單的啟發式算法。近期研究（如 CausalRCA, Nezha）開始強調利用 PC 算法或限制性因果圖（Constrained Causal Graphs）來排除虛假相關，確保 RCA 的結果具備物理意義。
細粒度與多模態觀測（Fine-grained & Multi-modal Analysis）：
研究重點正從單純的「服務級別（Service-level）」定位轉向「指標級別（Metric-level）」定位。趨勢是結合 eBPF 技術獲取更底層的內核指標，並透過異質圖網路（Heterogeneous Graphs）同時建模 Traces 與 Metrics，以應對更隱蔽的性能退化問題。
追求內在可解釋性與穩健性（Interpretability & Robustness）：
隨著模型複雜度提升，學界開始反思「後驗解釋（Post-hoc Explainability，如 LIME/SHAP 局部代理）」的不足，轉而研發「內在可解釋（Intrinsic Interpretability）」的模型架構。同時，如何對抗數據雜訊、排除後驗洩漏（Posterior Leakage），並在小樣本或零樣本故障場景下保持穩健，亦是目前的主流研究方向。
計畫目的 
本計畫擬結合 GNN_KAN 的內在可解釋性 與 CPG-Shap 的因果歸因框架，開發一套能克服「根因掩蓋」並提供「數學證明式解釋」的自動化 RCA 系統，旨在提升微服務系統的平均修復時間（MTTR）並確保維運決策的透明度。


在本計畫中，我們將研究方法分為四大階段，旨在從基礎的可解釋性架構開發，進階到多模態數據融合，最後達成公理化的根因歸因。以下為各階段的詳細執行步驟、原理與技術闡述：
第一階段：建構內在可解釋之圖表徵學習架構 (GNN-KAN)
本階段目標是解決傳統深度學習在維運場景中的「黑箱」問題，建立具備數學透明度的表徵模型。
執行步驟：
KAN 層級設計：捨棄傳統 GNN 中的多層感知器（MLP），改以 Kolmogorov-Arnold Networks (KAN) 作為節點更新與訊息傳遞的核心。
基底函數擬合：在圖結構的每一條邊上部署可學習的一維基底函數（如 B-Splines 或 Chebyshev 多項式）。
拓撲特徵融合：透過聚合鄰居節點的狀態，利用 KAN 的非線性擬合能力捕捉服務間複雜的異常傳播模式。
相關原理與技術：
Kolmogorov-Arnold 表示定理：該定理指出任何多變量連續函數都能表示為一維函數的有限合成。相較於 MLP 的「權重在節點上」，KAN 的「權重在邊上的函數」，這使得模型可以將高維的異常特徵解構成可視化的曲線，維運人員可直接觀察特定指標（如 CPU 使用率）如何非線性地影響下游服務的延遲。
第二階段：領域感知特徵工程與排除後驗洩漏
本階段專注於數據質量的優化，確保模型學習到的是真正的「因果先兆」而非「事後結果」。
執行步驟：
特徵篩選與時間對齊：針對 Metrics（指標）數據進行滑動窗口處理，區分故障發生前、中、後的數據特徵。
後驗洩漏檢測：建立「前饋可觀測性」準則，系統性移除那些僅在故障發生後才產生劇烈波動、但不具備預測價值的特徵。
穩健性特徵提取：利用絕對中位差（MAD）取代標準差來處理離群值，提升模型對雜訊數據的抵抗力。
相關原理與技術：
Domain-aware Feature Engineering：結合微服務領域知識（如調用鏈深度、扇入扇出數），將原始監控指標轉化為具有物理意義的輸入，減少模型在無關特徵上的過擬合。
第三階段：混合因果傳播圖（CPG）之建模與發現
本階段旨在整合異質數據，建構一個能真實反映故障路徑的動態拓撲圖。
執行步驟：
確定性拓撲約束：利用分散式追蹤（Traces）提取服務間的實體調用關係，作為因果圖的骨幹。
機率性因果發現：在骨幹之上，對 Metrics 進行因果分析（Causal Discovery），利用統計方法判斷指標間的領先與落後關係。
加權傳播邊建構：根據故障傳播的概率與強度，為每一條依賴邊賦予權重，形成加權因果傳播圖（CPG）。
相關原理與技術：
PC 算法 (Constraint-based Causal Discovery)：透過條件獨立性檢定來推導變量間的因果方向，解決「相關性不等於因果性」的問題，精確識別異常是從 A 傳向 B，還是由共同原因引起。
第四階段：基於賽局理論之公理化責任歸因 (Shapley Attribution)
最後階段將利用嚴謹的數學方法，從 CPG 圖中精確量化每個節點對故障結果的貢獻度。
執行步驟：
定義異常收益函數：將系統的總體異常（如終端響應延遲）定義為賽局中的「總收益」。
子集邊際貢獻計算：模擬不同服務節點「參與」或「不參與」故障傳播時，對整體異常程度的影響。
Shapley 值分配：計算各節點的平均邊際貢獻，以此作為根因評分，產出最終的根因排序名單。
相關原理與技術：
Shapley Value（賽局歸因）：這是一種具備公理化特性（有效性、對稱性、 dummy 特性）的分派方法。在微服務中，下游服務往往症狀最明顯，但 Shapley 值能透過消除「連鎖效應」的影響，將最高分分配給真正引起故障的上游節點，徹底解決「根因掩蓋」問題。
執行進度規劃 (二年期)
期間
階段
主要交付成果
第一年 (1-6月)
階段一：GNN-KAN 開發
完成內在可解釋模型原型與基底函數可視化模組。
第一年 (7-12月)
階段二：特徵工程優化
建立排除後驗洩漏的特徵庫，完成初步模型效能驗證。
第二年 (1-6月)
階段三：CPG 因果建模
整合 Traces 與 Metrics，實現動態因果圖自動生成。
第二年 (7-12月)
階段四：Shapley 歸因與實證
在 RCAEval 等基準平台完成大規模測試，產出研究論文。



